(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/smart-account-kit/dist/storage/memory.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * In-Memory Storage Adapter
 *
 * Simple in-memory storage for credentials. Useful for testing or
 * server-side environments where persistence isn't needed.
 *
 * WARNING: Data is lost when the application restarts.
 */ __turbopack_context__.s([
    "MemoryStorage",
    ()=>MemoryStorage
]);
class MemoryStorage {
    credentials = new Map();
    session = null;
    async save(credential) {
        this.credentials.set(credential.credentialId, {
            ...credential
        });
    }
    async get(credentialId) {
        const credential = this.credentials.get(credentialId);
        return credential ? {
            ...credential
        } : null;
    }
    async getByContract(contractId) {
        const results = [];
        for (const credential of this.credentials.values()){
            if (credential.contractId === contractId) {
                results.push({
                    ...credential
                });
            }
        }
        return results;
    }
    async getAll() {
        return Array.from(this.credentials.values()).map((c)=>({
                ...c
            }));
    }
    async delete(credentialId) {
        this.credentials.delete(credentialId);
    }
    async update(credentialId, updates) {
        const credential = this.credentials.get(credentialId);
        if (credential) {
            this.credentials.set(credentialId, {
                ...credential,
                ...updates
            });
        }
    }
    async clear() {
        this.credentials.clear();
        this.session = null;
    }
    async saveSession(session) {
        this.session = {
            ...session
        };
    }
    async getSession() {
        return this.session ? {
            ...this.session
        } : null;
    }
    async clearSession() {
        this.session = null;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Constants used throughout the Smart Account Kit SDK.
 *
 * @packageDocumentation
 */ // ============================================================================
// WebAuthn Configuration
// ============================================================================
/** Default timeout for WebAuthn operations in milliseconds */ __turbopack_context__.s([
    "API_PATH_CONTRACT",
    ()=>API_PATH_CONTRACT,
    "API_PATH_LOOKUP",
    ()=>API_PATH_LOOKUP,
    "API_PATH_LOOKUP_ADDRESS",
    ()=>API_PATH_LOOKUP_ADDRESS,
    "API_PATH_STATS",
    ()=>API_PATH_STATS,
    "AUTH_ENTRY_EXPIRATION_BUFFER",
    ()=>AUTH_ENTRY_EXPIRATION_BUFFER,
    "BASE_FEE",
    ()=>BASE_FEE,
    "DB_NAME",
    ()=>DB_NAME,
    "DB_VERSION",
    ()=>DB_VERSION,
    "DEFAULT_INDEXER_TIMEOUT_MS",
    ()=>DEFAULT_INDEXER_TIMEOUT_MS,
    "DEFAULT_RELAYER_TIMEOUT_MS",
    ()=>DEFAULT_RELAYER_TIMEOUT_MS,
    "DEFAULT_SESSION_EXPIRY_MS",
    ()=>DEFAULT_SESSION_EXPIRY_MS,
    "FRIENDBOT_RESERVE_XLM",
    ()=>FRIENDBOT_RESERVE_XLM,
    "FRIENDBOT_URL",
    ()=>FRIENDBOT_URL,
    "IDB_INDEX_CONTRACT_ID",
    ()=>IDB_INDEX_CONTRACT_ID,
    "IDB_INDEX_CREATED_AT",
    ()=>IDB_INDEX_CREATED_AT,
    "IDB_INDEX_IS_PRIMARY",
    ()=>IDB_INDEX_IS_PRIMARY,
    "IDB_SESSION_KEY",
    ()=>IDB_SESSION_KEY,
    "IDB_STORE_CREDENTIALS",
    ()=>IDB_STORE_CREDENTIALS,
    "IDB_STORE_SESSION",
    ()=>IDB_STORE_SESSION,
    "LEDGERS_PER_DAY",
    ()=>LEDGERS_PER_DAY,
    "LEDGERS_PER_HOUR",
    ()=>LEDGERS_PER_HOUR,
    "LEDGERS_PER_WEEK",
    ()=>LEDGERS_PER_WEEK,
    "LOCALSTORAGE_CREDENTIALS_KEY",
    ()=>LOCALSTORAGE_CREDENTIALS_KEY,
    "LOCALSTORAGE_SESSION_KEY",
    ()=>LOCALSTORAGE_SESSION_KEY,
    "SECP256R1_PUBLIC_KEY_SIZE",
    ()=>SECP256R1_PUBLIC_KEY_SIZE,
    "STROOPS_PER_XLM",
    ()=>STROOPS_PER_XLM,
    "UNCOMPRESSED_PUBKEY_PREFIX",
    ()=>UNCOMPRESSED_PUBKEY_PREFIX,
    "WEBAUTHN_TIMEOUT_MS",
    ()=>WEBAUTHN_TIMEOUT_MS
]);
const WEBAUTHN_TIMEOUT_MS = 60000;
const BASE_FEE = "100";
const STROOPS_PER_XLM = 10_000_000;
const FRIENDBOT_RESERVE_XLM = 5;
const SECP256R1_PUBLIC_KEY_SIZE = 65;
const UNCOMPRESSED_PUBKEY_PREFIX = 0x04;
const DB_NAME = "smart-account-kit";
const DB_VERSION = 2;
const LOCALSTORAGE_CREDENTIALS_KEY = "smart-account-kit:credentials";
const LOCALSTORAGE_SESSION_KEY = "smart-account-kit:session";
const DEFAULT_SESSION_EXPIRY_MS = 7 * 24 * 60 * 60 * 1000;
const LEDGERS_PER_HOUR = 720;
const LEDGERS_PER_DAY = 17280;
const LEDGERS_PER_WEEK = 120960;
const AUTH_ENTRY_EXPIRATION_BUFFER = 100;
const FRIENDBOT_URL = "https://friendbot.stellar.org";
const DEFAULT_INDEXER_TIMEOUT_MS = 10000;
const DEFAULT_RELAYER_TIMEOUT_MS = 360000;
const IDB_STORE_CREDENTIALS = "credentials";
const IDB_STORE_SESSION = "session";
const IDB_SESSION_KEY = "current";
const IDB_INDEX_CONTRACT_ID = "contractId";
const IDB_INDEX_CREATED_AT = "createdAt";
const IDB_INDEX_IS_PRIMARY = "isPrimary";
const API_PATH_LOOKUP = "/api/lookup";
const API_PATH_LOOKUP_ADDRESS = "/api/lookup/address";
const API_PATH_CONTRACT = "/api/contract";
const API_PATH_STATS = "/api/stats";
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/errors.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Custom error classes for the Smart Account Kit SDK.
 *
 * These provide structured error handling with error codes and context.
 *
 * @packageDocumentation
 */ /**
 * Error codes for Smart Account Kit operations.
 */ __turbopack_context__.s([
    "CredentialNotFoundError",
    ()=>CredentialNotFoundError,
    "SessionError",
    ()=>SessionError,
    "SignerNotFoundError",
    ()=>SignerNotFoundError,
    "SimulationError",
    ()=>SimulationError,
    "SmartAccountError",
    ()=>SmartAccountError,
    "SmartAccountErrorCode",
    ()=>SmartAccountErrorCode,
    "SubmissionError",
    ()=>SubmissionError,
    "ValidationError",
    ()=>ValidationError,
    "WalletNotConnectedError",
    ()=>WalletNotConnectedError,
    "WebAuthnError",
    ()=>WebAuthnError,
    "wrapError",
    ()=>wrapError
]);
var SmartAccountErrorCode;
(function(SmartAccountErrorCode) {
    // Configuration errors (1xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["INVALID_CONFIG"] = 1001] = "INVALID_CONFIG";
    SmartAccountErrorCode[SmartAccountErrorCode["MISSING_CONFIG"] = 1002] = "MISSING_CONFIG";
    // Wallet state errors (2xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["WALLET_NOT_CONNECTED"] = 2001] = "WALLET_NOT_CONNECTED";
    SmartAccountErrorCode[SmartAccountErrorCode["WALLET_ALREADY_EXISTS"] = 2002] = "WALLET_ALREADY_EXISTS";
    SmartAccountErrorCode[SmartAccountErrorCode["WALLET_NOT_FOUND"] = 2003] = "WALLET_NOT_FOUND";
    // Credential errors (3xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["CREDENTIAL_NOT_FOUND"] = 3001] = "CREDENTIAL_NOT_FOUND";
    SmartAccountErrorCode[SmartAccountErrorCode["CREDENTIAL_ALREADY_EXISTS"] = 3002] = "CREDENTIAL_ALREADY_EXISTS";
    SmartAccountErrorCode[SmartAccountErrorCode["CREDENTIAL_INVALID"] = 3003] = "CREDENTIAL_INVALID";
    SmartAccountErrorCode[SmartAccountErrorCode["CREDENTIAL_DEPLOYMENT_FAILED"] = 3004] = "CREDENTIAL_DEPLOYMENT_FAILED";
    // WebAuthn errors (4xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["WEBAUTHN_REGISTRATION_FAILED"] = 4001] = "WEBAUTHN_REGISTRATION_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["WEBAUTHN_AUTHENTICATION_FAILED"] = 4002] = "WEBAUTHN_AUTHENTICATION_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["WEBAUTHN_NOT_SUPPORTED"] = 4003] = "WEBAUTHN_NOT_SUPPORTED";
    SmartAccountErrorCode[SmartAccountErrorCode["WEBAUTHN_CANCELLED"] = 4004] = "WEBAUTHN_CANCELLED";
    // Transaction errors (5xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["TRANSACTION_SIMULATION_FAILED"] = 5001] = "TRANSACTION_SIMULATION_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["TRANSACTION_SIGNING_FAILED"] = 5002] = "TRANSACTION_SIGNING_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["TRANSACTION_SUBMISSION_FAILED"] = 5003] = "TRANSACTION_SUBMISSION_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["TRANSACTION_TIMEOUT"] = 5004] = "TRANSACTION_TIMEOUT";
    // Signer errors (6xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["SIGNER_NOT_FOUND"] = 6001] = "SIGNER_NOT_FOUND";
    SmartAccountErrorCode[SmartAccountErrorCode["SIGNER_INVALID"] = 6002] = "SIGNER_INVALID";
    // Validation errors (7xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["INVALID_ADDRESS"] = 7001] = "INVALID_ADDRESS";
    SmartAccountErrorCode[SmartAccountErrorCode["INVALID_AMOUNT"] = 7002] = "INVALID_AMOUNT";
    SmartAccountErrorCode[SmartAccountErrorCode["INVALID_INPUT"] = 7003] = "INVALID_INPUT";
    // Storage errors (8xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["STORAGE_READ_FAILED"] = 8001] = "STORAGE_READ_FAILED";
    SmartAccountErrorCode[SmartAccountErrorCode["STORAGE_WRITE_FAILED"] = 8002] = "STORAGE_WRITE_FAILED";
    // Session errors (9xxx)
    SmartAccountErrorCode[SmartAccountErrorCode["SESSION_EXPIRED"] = 9001] = "SESSION_EXPIRED";
    SmartAccountErrorCode[SmartAccountErrorCode["SESSION_INVALID"] = 9002] = "SESSION_INVALID";
})(SmartAccountErrorCode || (SmartAccountErrorCode = {}));
class SmartAccountError extends Error {
    /** Error code for programmatic error handling */ code;
    /** Additional context about the error */ context;
    /** Original error that caused this error */ cause;
    constructor(message, code, options){
        super(message);
        this.name = "SmartAccountError";
        this.code = code;
        this.context = options?.context;
        this.cause = options?.cause;
        // Maintain proper stack trace in V8 environments
        if (Error.captureStackTrace) {
            Error.captureStackTrace(this, SmartAccountError);
        }
    }
    /**
     * Create a formatted error message with code and context.
     */ toDetailedString() {
        let msg = `[${this.code}] ${this.message}`;
        if (this.context) {
            msg += `\nContext: ${JSON.stringify(this.context, null, 2)}`;
        }
        if (this.cause) {
            msg += `\nCaused by: ${this.cause.message}`;
        }
        return msg;
    }
}
class WalletNotConnectedError extends SmartAccountError {
    constructor(operation){
        super(operation ? `Wallet must be connected to ${operation}` : "Wallet not connected", SmartAccountErrorCode.WALLET_NOT_CONNECTED, {
            context: operation ? {
                operation
            } : undefined
        });
        this.name = "WalletNotConnectedError";
    }
}
class CredentialNotFoundError extends SmartAccountError {
    constructor(credentialId){
        super(`Credential not found: ${credentialId}`, SmartAccountErrorCode.CREDENTIAL_NOT_FOUND, {
            context: {
                credentialId
            }
        });
        this.name = "CredentialNotFoundError";
    }
}
class SignerNotFoundError extends SmartAccountError {
    constructor(identifier){
        super(`No signer found for: ${identifier}`, SmartAccountErrorCode.SIGNER_NOT_FOUND, {
            context: {
                identifier
            }
        });
        this.name = "SignerNotFoundError";
    }
}
class SimulationError extends SmartAccountError {
    constructor(message, details){
        super(message, SmartAccountErrorCode.TRANSACTION_SIMULATION_FAILED, {
            context: details
        });
        this.name = "SimulationError";
    }
}
class SubmissionError extends SmartAccountError {
    constructor(message, hash, details){
        super(message, SmartAccountErrorCode.TRANSACTION_SUBMISSION_FAILED, {
            context: {
                hash,
                ...details
            }
        });
        this.name = "SubmissionError";
    }
}
class ValidationError extends SmartAccountError {
    constructor(message, code = SmartAccountErrorCode.INVALID_INPUT, context){
        super(message, code, {
            context
        });
        this.name = "ValidationError";
    }
}
class WebAuthnError extends SmartAccountError {
    constructor(message, code, cause){
        super(message, code, {
            cause
        });
        this.name = "WebAuthnError";
    }
}
class SessionError extends SmartAccountError {
    constructor(message, code = SmartAccountErrorCode.SESSION_INVALID){
        super(message, code);
        this.name = "SessionError";
    }
}
function wrapError(err, defaultCode = SmartAccountErrorCode.INVALID_INPUT) {
    if (err instanceof SmartAccountError) {
        return err;
    }
    const message = err instanceof Error ? err.message : String(err);
    const cause = err instanceof Error ? err : undefined;
    return new SmartAccountError(message, defaultCode, {
        cause
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildKeyData",
    ()=>buildKeyData,
    "compactSignature",
    ()=>compactSignature,
    "deriveContractAddress",
    ()=>deriveContractAddress,
    "extractCredentialIdFromKeyData",
    ()=>extractCredentialIdFromKeyData,
    "extractPubkeyFromKeyData",
    ()=>extractPubkeyFromKeyData,
    "extractPublicKeyFromAttestation",
    ()=>extractPublicKeyFromAttestation,
    "generateChallenge",
    ()=>generateChallenge,
    "stroopsToXlm",
    ()=>stroopsToXlm,
    "validateAddress",
    ()=>validateAddress,
    "validateAmount",
    ()=>validateAmount,
    "validateNotEmpty",
    ()=>validateNotEmpty,
    "xlmToStroops",
    ()=>xlmToStroops
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
/**
 * Utility functions for the Smart Account Kit SDK.
 *
 * Contains cryptographic helpers, validation functions, and common operations.
 *
 * @packageDocumentation
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/errors.js [app-client] (ecmascript)");
;
;
;
;
function validateAddress(address, fieldName = "address") {
    if (!address || typeof address !== "string") {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`${fieldName} is required`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_ADDRESS, {
            field: fieldName
        });
    }
    const isValidAccount = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["StrKey"].isValidEd25519PublicKey(address);
    const isValidContract = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["StrKey"].isValidContract(address);
    if (!isValidAccount && !isValidContract) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`Invalid ${fieldName}: must be a valid Stellar account (G...) or contract (C...) address`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_ADDRESS, {
            field: fieldName,
            value: address.slice(0, 10) + "..."
        });
    }
}
function validateAmount(amount, fieldName = "amount") {
    if (typeof amount !== "number" || !Number.isFinite(amount)) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`${fieldName} must be a number`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_AMOUNT, {
            field: fieldName
        });
    }
    if (amount <= 0) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`${fieldName} must be positive`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_AMOUNT, {
            field: fieldName,
            value: amount
        });
    }
}
function validateNotEmpty(value, fieldName) {
    if (!value || typeof value !== "string" || value.trim().length === 0) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`${fieldName} is required`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            field: fieldName
        });
    }
}
function xlmToStroops(xlm) {
    return BigInt(Math.round(xlm * __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["STROOPS_PER_XLM"]));
}
function stroopsToXlm(stroops) {
    return Number(stroops) / __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["STROOPS_PER_XLM"];
}
function buildKeyData(publicKey, credentialId) {
    const credentialIdBuffer = typeof credentialId === "string" ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId) : credentialId;
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].concat([
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(publicKey),
        credentialIdBuffer
    ]);
}
function extractPubkeyFromKeyData(keyData) {
    return keyData.slice(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]);
}
function extractCredentialIdFromKeyData(keyData) {
    return keyData.slice(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]);
}
function deriveContractAddress(credentialId, deployerPublicKey, networkPassphrase) {
    const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeContractId(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageContractId({
        networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(networkPassphrase)),
        contractIdPreimage: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ContractIdPreimage.contractIdPreimageFromAddress(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ContractIdPreimageFromAddress({
            address: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(deployerPublicKey).toScAddress(),
            salt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(credentialId)
        }))
    }));
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["StrKey"].encodeContract((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(preimage.toXDR()));
}
async function extractPublicKeyFromAttestation(response) {
    let publicKey;
    // Try to get the public key from the response directly
    if (response.publicKey) {
        publicKey = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(response.publicKey);
        publicKey = publicKey.slice(publicKey.length - __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]);
    }
    // Validate it's a proper uncompressed EC point
    if (!publicKey || publicKey[0] !== __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["UNCOMPRESSED_PUBKEY_PREFIX"] || publicKey.length !== __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]) {
        // Fall back to extracting from authenticatorData or attestationObject
        let x;
        let y;
        if (response.authenticatorData) {
            const authenticatorData = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(response.authenticatorData);
            const credentialIdLength = authenticatorData[53] << 8 | authenticatorData[54];
            x = authenticatorData.slice(65 + credentialIdLength, 97 + credentialIdLength);
            y = authenticatorData.slice(100 + credentialIdLength, 132 + credentialIdLength);
        } else if (response.attestationObject) {
            const attestationObject = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(response.attestationObject);
            // COSE key structure prefix for ES256 (P-256)
            const publicKeyPrefixSlice = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from([
                0xa5,
                0x01,
                0x02,
                0x03,
                0x26,
                0x20,
                0x01,
                0x21,
                0x58,
                0x20
            ]);
            let startIndex = attestationObject.indexOf(publicKeyPrefixSlice);
            startIndex = startIndex + publicKeyPrefixSlice.length;
            x = attestationObject.slice(startIndex, 32 + startIndex);
            y = attestationObject.slice(35 + startIndex, 67 + startIndex);
        } else {
            throw new Error("Could not extract public key from attestation response");
        }
        publicKey = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from([
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["UNCOMPRESSED_PUBKEY_PREFIX"],
            ...x,
            ...y
        ]);
    }
    return new Uint8Array(publicKey);
}
function compactSignature(derSignature) {
    // Decode DER signature: 0x30 [total-length] 0x02 [r-length] [r] 0x02 [s-length] [s]
    let offset = 2; // Skip 0x30 and total length
    const rLength = derSignature[offset + 1];
    const r = derSignature.slice(offset + 2, offset + 2 + rLength);
    offset += 2 + rLength;
    const sLength = derSignature[offset + 1];
    const s = derSignature.slice(offset + 2, offset + 2 + sLength);
    // Convert to BigInt for low-S calculation
    const rBigInt = BigInt("0x" + r.toString("hex"));
    let sBigInt = BigInt("0x" + s.toString("hex"));
    // Ensure low-S form (required by Stellar)
    // n is the order of the secp256r1 curve
    const n = BigInt("0xffffffff00000000ffffffffffffffffbce6faada7179e84f3b9cac2fc632551");
    const halfN = n / 2n;
    if (sBigInt > halfN) {
        sBigInt = n - sBigInt;
    }
    // Convert back to 32-byte buffers (padded)
    const rPadded = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(rBigInt.toString(16).padStart(64, "0"), "hex");
    const sLowS = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(sBigInt.toString(16).padStart(64, "0"), "hex");
    return new Uint8Array(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].concat([
        rPadded,
        sLowS
    ]));
}
function generateChallenge() {
    const bytes = new Uint8Array(32);
    crypto.getRandomValues(bytes);
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].encode(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(bytes));
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/events.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Event system for Smart Account Kit.
 *
 * Provides a simple event emitter for credential lifecycle events.
 *
 * @packageDocumentation
 */ // ============================================================================
// Event Emitter
// ============================================================================
/**
 * Simple event emitter for Smart Account Kit events.
 *
 * @example
 * ```typescript
 * const emitter = new SmartAccountEventEmitter();
 *
 * // Subscribe to events
 * emitter.on('walletConnected', ({ contractId }) => {
 *   console.log('Connected to wallet:', contractId);
 * });
 *
 * // Emit an event
 * emitter.emit('walletConnected', { contractId: 'C...', credentialId: '...' });
 * ```
 */ __turbopack_context__.s([
    "SmartAccountEventEmitter",
    ()=>SmartAccountEventEmitter
]);
class SmartAccountEventEmitter {
    listeners = new Map();
    /** Optional error handler for listener errors */ errorHandler;
    /**
     * Set an error handler for listener errors.
     * By default, listener errors are silently caught.
     *
     * @param handler - Error handler function, or undefined to disable
     */ setErrorHandler(handler) {
        this.errorHandler = handler;
    }
    /**
     * Subscribe to an event.
     *
     * @param event - The event to subscribe to
     * @param listener - The callback function
     * @returns An unsubscribe function
     */ on(event, listener) {
        if (!this.listeners.has(event)) {
            this.listeners.set(event, new Set());
        }
        const listeners = this.listeners.get(event);
        listeners.add(listener);
        // Return unsubscribe function
        return ()=>{
            listeners.delete(listener);
        };
    }
    /**
     * Subscribe to an event, but only trigger once.
     *
     * @param event - The event to subscribe to
     * @param listener - The callback function
     * @returns An unsubscribe function
     */ once(event, listener) {
        const unsubscribe = this.on(event, (data)=>{
            unsubscribe();
            listener(data);
        });
        return unsubscribe;
    }
    /**
     * Unsubscribe from an event.
     *
     * @param event - The event to unsubscribe from
     * @param listener - The callback function to remove
     */ off(event, listener) {
        const listeners = this.listeners.get(event);
        if (listeners) {
            listeners.delete(listener);
        }
    }
    /**
     * Emit an event to all subscribers.
     *
     * Listener errors are caught to prevent one failing listener from
     * affecting others. If you need error handling, wrap your listener.
     *
     * @param event - The event to emit
     * @param data - The event data
     */ emit(event, data) {
        const listeners = this.listeners.get(event);
        if (listeners) {
            for (const listener of listeners){
                try {
                    listener(data);
                } catch (err) {
                    // Call error handler if provided, otherwise silently catch
                    // to prevent one failing listener from affecting others
                    if (this.errorHandler) {
                        this.errorHandler(event, err);
                    }
                }
            }
        }
    }
    /**
     * Remove all listeners for a specific event, or all events if no event is specified.
     *
     * @param event - Optional event to clear listeners for
     */ removeAllListeners(event) {
        if (event) {
            this.listeners.delete(event);
        } else {
            this.listeners.clear();
        }
    }
    /**
     * Get the number of listeners for an event.
     *
     * @param event - The event to check
     * @returns The number of listeners
     */ listenerCount(event) {
        return this.listeners.get(event)?.size ?? 0;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/external-signers.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ExternalSignerManager",
    ()=>ExternalSignerManager
]);
/**
 * External Signer Manager
 *
 * Manages G-address signers (Stellar accounts) for multi-signature operations.
 * Supports two methods of adding signers:
 * 1. Raw secret key (Keypair) - stored in memory only
 * 2. External wallet via StellarWalletsKit (if installed)
 *
 * Wallet connections can be persisted to storage and auto-restored on init.
 *
 * @packageDocumentation
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
;
/** Storage key for persisted wallet connections */ const WALLET_STORAGE_KEY = "external_wallets";
class ExternalSignerManager {
    /** Keypair-based signers (memory-only, never persisted) */ keypairSigners = new Map();
    /** External wallet adapter (optional, for SWK integration) */ walletAdapter = null;
    /** Network passphrase for signing */ networkPassphrase;
    /** Storage for persisting wallet connections (optional) */ storage = null;
    /** Whether connections have been restored */ restored = false;
    constructor(networkPassphrase, walletAdapter, storage){
        this.networkPassphrase = networkPassphrase;
        this.walletAdapter = walletAdapter ?? null;
        this.storage = storage ?? null;
    }
    /**
     * Set or update the external wallet adapter
     */ setWalletAdapter(adapter) {
        this.walletAdapter = adapter;
    }
    /**
     * Add a signer from a raw secret key.
     *
     * The keypair is stored in memory only and is never persisted.
     * It will be lost when the page is refreshed.
     *
     * @param secretKey - Stellar secret key (S...)
     * @returns The derived public address
     * @throws Error if the secret key is invalid
     *
     * @example
     * ```typescript
     * const { address } = kit.externalSigners.addFromSecret("SCZANGBA5YHTNYVVV3C7CAZMTQDBJHJG6C34REYB6WBMG7CKKFJHYAEGQ");
     * console.log(`Added signer: ${address}`);
     * ```
     */ addFromSecret(secretKey) {
        // Validate and create keypair
        let keypair;
        try {
            keypair = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Keypair"].fromSecret(secretKey);
        } catch  {
            throw new Error("Invalid secret key. Must be a valid Stellar secret key (S...)");
        }
        const address = keypair.publicKey();
        // Store in memory
        this.keypairSigners.set(address, {
            keypair,
            address
        });
        return {
            address
        };
    }
    /**
     * Add a signer from an external wallet (Freighter, Lobstr, etc.)
     *
     * Requires StellarWalletsKit to be installed and the adapter to be initialized.
     * Shows the wallet selection modal and tracks the connected wallet.
     * If storage is configured, the connection is persisted for auto-restore.
     *
     * @returns Connected wallet info, or null if cancelled/unavailable
     * @throws Error if no wallet adapter is configured
     *
     * @example
     * ```typescript
     * const wallet = await kit.externalSigners.addFromWallet();
     * if (wallet) {
     *   console.log(`Connected: ${wallet.walletName} (${wallet.address})`);
     * }
     * ```
     */ async addFromWallet() {
        if (!this.walletAdapter) {
            throw new Error("No wallet adapter configured. Install @creit-tech/stellar-wallets-kit " + "and pass a StellarWalletsKitAdapter to the SDK config.");
        }
        const wallet = await this.walletAdapter.connect();
        // Persist the connection if storage is configured
        if (wallet && this.storage) {
            this.saveWalletToStorage(wallet);
        }
        return wallet;
    }
    /**
     * Restore previously connected wallets from storage.
     *
     * Attempts to reconnect to all wallets that were saved in storage.
     * This is called automatically if storage is configured, but can also
     * be called manually.
     *
     * @returns Array of successfully restored wallet connections
     *
     * @example
     * ```typescript
     * const restored = await kit.externalSigners.restoreConnections();
     * console.log(`Restored ${restored.length} wallet connections`);
     * ```
     */ async restoreConnections() {
        if (this.restored) {
            // Already restored, return current wallet connections
            return this.walletAdapter?.getConnectedWallets() ?? [];
        }
        this.restored = true;
        if (!this.storage || !this.walletAdapter?.reconnect) {
            return [];
        }
        const stored = this.getStoredWallets();
        const restored = [];
        for (const savedWallet of stored){
            try {
                const wallet = await this.walletAdapter.reconnect(savedWallet.walletId);
                if (wallet) {
                    restored.push(wallet);
                } else {
                    // Reconnection failed - remove stale entry from storage
                    this.removeWalletFromStorage(savedWallet.address);
                }
            } catch  {
                // Reconnection failed - remove stale entry from storage
                this.removeWalletFromStorage(savedWallet.address);
            }
        }
        return restored;
    }
    /**
     * Remove a signer by address.
     *
     * For keypair signers, this removes the keypair from memory.
     * For wallet signers, this disconnects the wallet and removes from storage.
     *
     * @param address - The G-address to remove
     */ remove(address) {
        // Remove from keypair signers
        this.keypairSigners.delete(address);
        // Remove from wallet adapter if it has the method
        const adapter = this.walletAdapter;
        if (adapter?.disconnectByAddress) {
            adapter.disconnectByAddress(address);
        }
        // Remove from storage
        this.removeWalletFromStorage(address);
    }
    // ===========================================================================
    // Private Storage Helpers
    // ===========================================================================
    /**
     * Get stored wallet connections from storage
     */ getStoredWallets() {
        if (!this.storage) return [];
        try {
            const data = this.storage.getItem(WALLET_STORAGE_KEY);
            if (!data) return [];
            return JSON.parse(data);
        } catch  {
            return [];
        }
    }
    /**
     * Save a wallet connection to storage
     */ saveWalletToStorage(wallet) {
        if (!this.storage) return;
        const stored = this.getStoredWallets();
        // Remove existing entry for this address (if any)
        const filtered = stored.filter((w)=>w.address !== wallet.address);
        // Add new entry
        filtered.push({
            address: wallet.address,
            walletId: wallet.walletId,
            walletName: wallet.walletName,
            connectedAt: Date.now()
        });
        this.storage.setItem(WALLET_STORAGE_KEY, JSON.stringify(filtered));
    }
    /**
     * Remove a wallet connection from storage
     */ removeWalletFromStorage(address) {
        if (!this.storage) return;
        const stored = this.getStoredWallets();
        const filtered = stored.filter((w)=>w.address !== address);
        if (filtered.length === 0) {
            this.storage.removeItem(WALLET_STORAGE_KEY);
        } else {
            this.storage.setItem(WALLET_STORAGE_KEY, JSON.stringify(filtered));
        }
    }
    /**
     * Remove all signers.
     *
     * Clears all keypair signers from memory and disconnects all wallets.
     */ async removeAll() {
        this.keypairSigners.clear();
        if (this.walletAdapter) {
            await this.walletAdapter.disconnect();
        }
        // Clear storage
        if (this.storage) {
            this.storage.removeItem(WALLET_STORAGE_KEY);
        }
    }
    /**
     * Get all registered external signers.
     *
     * @returns Array of external signer info
     */ getAll() {
        const signers = [];
        // Add keypair signers
        for (const [address] of this.keypairSigners){
            signers.push({
                address,
                type: "keypair"
            });
        }
        // Add wallet signers
        if (this.walletAdapter) {
            const wallets = this.walletAdapter.getConnectedWallets();
            for (const wallet of wallets){
                // Skip if already added as keypair (keypair takes precedence)
                if (!this.keypairSigners.has(wallet.address)) {
                    signers.push({
                        address: wallet.address,
                        type: "wallet",
                        walletName: wallet.walletName,
                        walletId: wallet.walletId
                    });
                }
            }
        }
        return signers;
    }
    /**
     * Check if we can sign for a specific address.
     *
     * @param address - The G-address to check
     * @returns True if we have a keypair or connected wallet for this address
     */ canSignFor(address) {
        // Check keypair signers first
        if (this.keypairSigners.has(address)) {
            return true;
        }
        // Check wallet adapter
        if (this.walletAdapter?.canSignFor(address)) {
            return true;
        }
        return false;
    }
    /**
     * Get signer info for a specific address.
     *
     * @param address - The G-address to look up
     * @returns Signer info if found, undefined otherwise
     */ get(address) {
        // Check keypair signers
        if (this.keypairSigners.has(address)) {
            return {
                address,
                type: "keypair"
            };
        }
        // Check wallet adapter
        if (this.walletAdapter) {
            const wallet = this.walletAdapter.getWalletForAddress?.(address);
            if (wallet) {
                return {
                    address: wallet.address,
                    type: "wallet",
                    walletName: wallet.walletName,
                    walletId: wallet.walletId
                };
            }
        }
        return undefined;
    }
    /**
     * Check if any external signers are registered.
     */ get hasSigners() {
        return this.keypairSigners.size > 0 || (this.walletAdapter?.getConnectedWallets().length ?? 0) > 0;
    }
    /**
     * Sign an auth entry preimage with an external signer.
     *
     * For keypair signers, signs directly with the Keypair.
     * For wallet signers, delegates to the wallet adapter.
     *
     * @param preimageXdr - Base64-encoded HashIdPreimage XDR
     * @param address - The G-address to sign with
     * @returns Base64-encoded signature
     * @throws Error if no signer is available for the address
     *
     * @internal Used by the SDK during multi-signer operations
     */ async signAuthEntry(preimageXdr, address) {
        // Try keypair signer first
        const keypairSigner = this.keypairSigners.get(address);
        if (keypairSigner) {
            // Parse the preimage and sign
            const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.fromXDR(preimageXdr, "base64");
            const payload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(preimage.toXDR());
            const signature = keypairSigner.keypair.sign(payload);
            return {
                signedAuthEntry: signature.toString("base64"),
                signerAddress: address
            };
        }
        // Try wallet adapter
        if (this.walletAdapter?.canSignFor(address)) {
            const result = await this.walletAdapter.signAuthEntry(preimageXdr, {
                networkPassphrase: this.networkPassphrase,
                address
            });
            return {
                signedAuthEntry: result.signedAuthEntry,
                signerAddress: result.signerAddress ?? address
            };
        }
        throw new Error(`No signer available for address: ${address}`);
    }
    /**
     * Check if the wallet adapter is available and initialized.
     */ get hasWalletAdapter() {
        return this.walletAdapter !== null;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/indexer.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_INDEXER_URLS",
    ()=>DEFAULT_INDEXER_URLS,
    "IndexerClient",
    ()=>IndexerClient,
    "IndexerError",
    ()=>IndexerError
]);
/**
 * Indexer Client for Smart Account Kit
 *
 * Provides reverse lookups from signers (credential IDs, addresses) to smart account contracts.
 * This enables discovering which contracts a user has access to based on their signer credentials.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
const DEFAULT_INDEXER_URLS = {
    "Test SDF Network ; September 2015": "https://smart-account-indexer.sdf-ecosystem.workers.dev"
};
class IndexerClient {
    baseUrl;
    timeout;
    constructor(config){
        // Remove trailing slash if present
        this.baseUrl = config.baseUrl.replace(/\/$/, "");
        this.timeout = config.timeout ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_INDEXER_TIMEOUT_MS"];
    }
    /**
     * Create an IndexerClient for a specific network passphrase.
     * Uses the default indexer URL for known networks.
     *
     * @param networkPassphrase - The Stellar network passphrase
     * @returns IndexerClient configured for the network, or null if no default URL exists
     */ static forNetwork(networkPassphrase) {
        const url = DEFAULT_INDEXER_URLS[networkPassphrase];
        if (!url) return null;
        return new IndexerClient({
            baseUrl: url
        });
    }
    /**
     * Look up smart account contracts by credential ID.
     *
     * This is the primary lookup method for passkey-based signers.
     * The credential ID comes from WebAuthn authentication.
     *
     * @param credentialId - Hex-encoded credential ID (from passkey)
     * @returns Contracts associated with this credential
     */ async lookupByCredentialId(credentialId) {
        // Ensure credential ID is lowercase hex
        const normalizedId = credentialId.toLowerCase().replace(/^0x/, "");
        const response = await this.fetch(`${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["API_PATH_LOOKUP"]}/${normalizedId}`);
        // Convert string counts to numbers (postgres returns strings for bigint)
        return {
            ...response,
            contracts: response.contracts.map(this.normalizeContractSummary)
        };
    }
    /**
     * Look up smart account contracts by signer address.
     *
     * This works for both:
     * - G-addresses (Delegated signers)
     * - C-addresses (External signer verifier contracts)
     *
     * @param address - Stellar address (G... or C...)
     * @returns Contracts associated with this address
     */ async lookupByAddress(address) {
        const response = await this.fetch(`${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["API_PATH_LOOKUP_ADDRESS"]}/${address}`);
        return {
            ...response,
            contracts: response.contracts.map(this.normalizeContractSummary)
        };
    }
    /**
     * Get detailed information about a smart account contract.
     *
     * Returns the current state including:
     * - Contract summary statistics
     * - Active context rules (excluding removed ones)
     * - Signers for each rule
     * - Policies for each rule
     *
     * @param contractId - Smart account contract address (C...)
     * @returns Contract details or null if not found
     */ async getContractDetails(contractId) {
        try {
            const response = await this.fetch(`${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["API_PATH_CONTRACT"]}/${contractId}`);
            return {
                ...response,
                summary: this.normalizeContractSummary(response.summary)
            };
        } catch (error) {
            // Return null for 404 (contract not found)
            if (error instanceof IndexerError && error.status === 404) {
                return null;
            }
            throw error;
        }
    }
    /**
     * Get indexer statistics.
     *
     * Useful for debugging and monitoring.
     */ async getStats() {
        return this.fetch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["API_PATH_STATS"]);
    }
    /**
     * Check if the indexer is healthy and reachable.
     */ async isHealthy() {
        try {
            const response = await this.fetch("/");
            return response.status === "ok";
        } catch  {
            return false;
        }
    }
    // ============================================================================
    // Private Methods
    // ============================================================================
    async fetch(path) {
        const controller = new AbortController();
        const timeoutId = setTimeout(()=>controller.abort(), this.timeout);
        try {
            const response = await fetch(`${this.baseUrl}${path}`, {
                headers: {
                    Accept: "application/json"
                },
                signal: controller.signal
            });
            if (!response.ok) {
                const errorBody = await response.text().catch(()=>"");
                throw new IndexerError(`Indexer request failed: ${response.status} ${response.statusText}`, response.status, errorBody);
            }
            return await response.json();
        } catch (error) {
            if (error instanceof IndexerError) {
                throw error;
            }
            if (error instanceof Error && error.name === "AbortError") {
                throw new IndexerError("Indexer request timed out", 0);
            }
            throw new IndexerError(`Indexer request failed: ${error instanceof Error ? error.message : String(error)}`, 0);
        } finally{
            clearTimeout(timeoutId);
        }
    }
    /**
     * Normalize contract summary counts from strings to numbers.
     * PostgreSQL returns bigint as strings in JSON.
     */ normalizeContractSummary(summary) {
        return {
            ...summary,
            context_rule_count: Number(summary.context_rule_count),
            external_signer_count: Number(summary.external_signer_count),
            delegated_signer_count: Number(summary.delegated_signer_count),
            native_signer_count: Number(summary.native_signer_count),
            first_seen_ledger: Number(summary.first_seen_ledger),
            last_seen_ledger: Number(summary.last_seen_ledger)
        };
    }
}
class IndexerError extends Error {
    status;
    body;
    constructor(message, status, body){
        super(message);
        this.status = status;
        this.body = body;
        this.name = "IndexerError";
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/version.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Package version - auto-generated from package.json
 * @packageDocumentation
 */ // This file is auto-generated. Do not edit manually.
// Run `pnpm run version:sync` to update.
__turbopack_context__.s([
    "NAME",
    ()=>NAME,
    "VERSION",
    ()=>VERSION
]);
const VERSION = "0.2.10";
const NAME = "smart-account-kit";
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/relayer.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RelayerClient",
    ()=>RelayerClient,
    "RelayerErrorCodes",
    ()=>RelayerErrorCodes
]);
/**
 * Relayer Client
 *
 * Client for submitting transactions via a Relayer proxy service.
 * The proxy handles communication with OpenZeppelin Relayer Channels,
 * managing API keys and providing CORS support for browser clients.
 *
 * Two submission modes:
 * 1. `send(func, auth)` - For Address credentials. Relayer builds the tx envelope.
 * 2. `sendXdr(xdr)` - For signed transactions. Relayer fee-bumps the signed tx.
 *
 * @see https://docs.openzeppelin.com/relayer/1.3.x/plugins/channels
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$version$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/version.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
;
const RelayerErrorCodes = {
    INVALID_PARAMS: "INVALID_PARAMS",
    INVALID_XDR: "INVALID_XDR",
    POOL_CAPACITY: "POOL_CAPACITY",
    SIMULATION_FAILED: "SIMULATION_FAILED",
    ONCHAIN_FAILED: "ONCHAIN_FAILED",
    INVALID_TIME_BOUNDS: "INVALID_TIME_BOUNDS",
    FEE_LIMIT_EXCEEDED: "FEE_LIMIT_EXCEEDED",
    UNAUTHORIZED: "UNAUTHORIZED"
};
class RelayerClient {
    url;
    timeout;
    // Default timeout of 6 minutes to accommodate testnet retries (up to 5 min)
    // when Relayer channel accounts need funding after testnet reset.
    // Mainnet requests return quickly; this only affects max wait time.
    constructor(url, timeout = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_RELAYER_TIMEOUT_MS"]){
        if (!url) {
            throw new Error("Relayer URL is required");
        }
        this.url = url.replace(/\/+$/, "");
        this.timeout = timeout;
    }
    /**
     * Check if the client is properly configured
     */ get isConfigured() {
        return !!this.url;
    }
    asObject(value) {
        if (!value || typeof value !== "object") {
            return null;
        }
        return value;
    }
    looksLikeErrorCode(value) {
        return /^[A-Z][A-Z0-9_:-]*$/.test(value.trim());
    }
    extractResponseData(responseData) {
        const root = this.asObject(responseData);
        if (!root) {
            return {};
        }
        const nested = this.asObject(root.data);
        return nested ?? root;
    }
    hasTransactionFields(value) {
        const data = this.asObject(value);
        if (!data) {
            return false;
        }
        return typeof data.transactionId === "string" || typeof data.hash === "string" || typeof data.status === "string";
    }
    isSuccessResponse(response, responseData) {
        const root = this.asObject(responseData);
        if (!root) {
            return false;
        }
        if (root.success === true) {
            return true;
        }
        if (!response.ok || root.success === false) {
            return false;
        }
        // Backward compatibility: some relayer proxies return tx fields directly
        // without a top-level `success` boolean.
        if (this.hasTransactionFields(root)) {
            return true;
        }
        const nested = this.asObject(root.data);
        return this.hasTransactionFields(nested);
    }
    extractErrorMessage(responseData, status) {
        const root = this.asObject(responseData);
        if (!root) {
            return `Relayer request failed with status ${status}`;
        }
        const message = typeof root.message === "string" ? root.message.trim() : "";
        if (message.length > 0) {
            return message;
        }
        const rawError = typeof root.error === "string" ? root.error.trim() : "";
        if (rawError.length > 0 && !this.looksLikeErrorCode(rawError)) {
            return rawError;
        }
        const nested = this.asObject(root.data);
        const nestedMessage = nested && typeof nested.message === "string" ? nested.message.trim() : "";
        if (nestedMessage.length > 0) {
            return nestedMessage;
        }
        if (rawError.length > 0) {
            return rawError;
        }
        return `Relayer request failed with status ${status}`;
    }
    toSuccessResult(responseData) {
        const data = this.extractResponseData(responseData);
        return {
            success: true,
            transactionId: typeof data.transactionId === "string" ? data.transactionId : undefined,
            hash: typeof data.hash === "string" ? data.hash : undefined,
            status: typeof data.status === "string" ? data.status : undefined
        };
    }
    toErrorResult(responseData, status) {
        const errorCode = this.extractErrorCode(responseData);
        return {
            success: false,
            error: this.extractErrorMessage(responseData, status),
            errorCode,
            details: this.extractResponseData(responseData)
        };
    }
    async submit(body, options) {
        const headers = {
            "Content-Type": "application/json",
            "X-Client-Name": __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$version$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["NAME"],
            "X-Client-Version": __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$version$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["VERSION"]
        };
        try {
            const controller = new AbortController();
            const timeout = options?.timeout ?? this.timeout;
            const timeoutId = setTimeout(()=>controller.abort(), timeout);
            const response = await fetch(this.url, {
                method: "POST",
                headers,
                body,
                signal: controller.signal
            });
            clearTimeout(timeoutId);
            let responseData = null;
            try {
                responseData = await response.json();
            } catch  {
                responseData = null;
            }
            if (this.isSuccessResponse(response, responseData)) {
                return this.toSuccessResult(responseData);
            }
            return this.toErrorResult(responseData, response.status);
        } catch (err) {
            if (err instanceof Error && err.name === "AbortError") {
                return {
                    success: false,
                    error: "Relayer request timed out",
                    errorCode: "TIMEOUT"
                };
            }
            return {
                success: false,
                error: err instanceof Error ? err.message : "Relayer request failed",
                details: err
            };
        }
    }
    /**
     * Submit a transaction via Relayer for fee sponsoring.
     *
     * The Relayer builds the transaction envelope using channel accounts and pays the fees.
     * Transactions are submitted in parallel using a pool of channel accounts.
     *
     * @param func - Base64 encoded Soroban host function XDR
     * @param auth - Array of base64 encoded authorization entry XDRs
     * @param options - Optional submission options
     * @returns The submission result
     *
     * @example
     * ```typescript
     * // Extract func and auth from a prepared transaction
     * const funcXdr = hostFunc.toXDR('base64');
     * const authXdrs = authEntries.map(e => e.toXDR('base64'));
     *
     * const result = await relayer.send(funcXdr, authXdrs);
     *
     * if (result.success) {
     *   console.log('Hash:', result.hash);
     * } else {
     *   console.error('Error:', result.error, result.errorCode);
     * }
     * ```
     */ async send(func, auth, options) {
        return this.submit(JSON.stringify({
            func,
            auth
        }), options);
    }
    /**
     * Submit a signed transaction for fee-bumping.
     *
     * Use this for transactions that require source_account auth (e.g., deployment).
     * The Relayer will fee-bump the signed transaction, preserving the inner signature.
     *
     * @param transaction - Signed transaction (Transaction object or XDR string)
     * @param options - Optional submission options
     * @returns The submission result
     *
     * @example
     * ```typescript
     * // Sign the deployment transaction
     * deployTx.sign(deployerKeypair);
     *
     * // Submit for fee-bumping
     * const result = await relayer.sendXdr(deployTx);
     * ```
     */ async sendXdr(transaction, options) {
        // Convert to XDR string
        const xdr = typeof transaction === "string" ? transaction : transaction.toXDR();
        return this.submit(JSON.stringify({
            xdr
        }), options);
    }
    /**
     * Extract error code from Relayer response
     */ extractErrorCode(responseData) {
        const data = this.asObject(responseData);
        if (!data) {
            return undefined;
        }
        // Check common error code locations
        if (typeof data.code === "string") {
            return data.code;
        }
        if (typeof data.errorCode === "string") {
            return data.errorCode;
        }
        if (data.data && typeof data.data === "object") {
            const nestedData = data.data;
            if (typeof nestedData.code === "string") {
                return nestedData.code;
            }
            if (typeof nestedData.errorCode === "string") {
                return nestedData.errorCode;
            }
            if (typeof nestedData.error === "string" && this.looksLikeErrorCode(nestedData.error)) {
                return nestedData.error;
            }
        }
        if (typeof data.error === "string" && this.looksLikeErrorCode(data.error)) {
            return data.error;
        }
        return undefined;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/signer-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SignerManager",
    ()=>SignerManager
]);
/**
 * Signer Manager
 *
 * Manages signers (passkeys and delegated accounts) for context rules.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
;
;
class SignerManager {
    deps;
    constructor(deps){
        this.deps = deps;
    }
    /**
     * Add a new passkey signer to a context rule.
     * Creates a new WebAuthn passkey and registers it as an External signer.
     */ async addPasskey(contextRuleId, appName, userName, options) {
        const { wallet, contractId } = this.deps.requireWallet();
        // Create the passkey
        const { rawResponse, credentialId, publicKey } = await this.deps.createPasskey(appName, userName);
        // Store the credential
        const storedCredential = {
            credentialId,
            publicKey,
            contractId,
            nickname: options?.nickname ?? `${userName} - ${new Date().toLocaleDateString()}`,
            createdAt: Date.now(),
            transports: rawResponse.response.transports,
            isPrimary: false,
            contextRuleId
        };
        await this.deps.storage.save(storedCredential);
        // Emit credential created event
        this.deps.events.emit("credentialCreated", {
            credential: storedCredential
        });
        // Build the External signer for the contract
        const keyData = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildKeyData"])(publicKey, credentialId);
        const signer = {
            tag: "External",
            values: [
                this.deps.webauthnVerifierAddress,
                keyData
            ]
        };
        // Build and return the add_signer transaction
        const transaction = await wallet.add_signer({
            context_rule_id: contextRuleId,
            signer
        });
        return {
            credentialId,
            publicKey,
            transaction
        };
    }
    /**
     * Add a delegated signer (Stellar account) to a context rule.
     */ async addDelegated(contextRuleId, publicKey) {
        const { wallet } = this.deps.requireWallet();
        const signer = {
            tag: "Delegated",
            values: [
                publicKey
            ]
        };
        return wallet.add_signer({
            context_rule_id: contextRuleId,
            signer
        });
    }
    /**
     * Remove a signer from a context rule.
     */ async remove(contextRuleId, signer) {
        const { wallet } = this.deps.requireWallet();
        // If it's an External signer (passkey), remove from local storage
        if (signer.tag === "External") {
            const keyData = signer.values[1];
            // Only try to delete if keyData contains a credential ID suffix
            if (keyData.length > __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]) {
                const credentialId = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].encode(keyData.slice(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]));
                await this.deps.storage.delete(credentialId);
            }
        }
        return wallet.remove_signer({
            context_rule_id: contextRuleId,
            signer
        });
    }
    /**
     * Remove a passkey signer by credential ID.
     */ async removePasskey(contextRuleId, credentialId) {
        const credential = await this.deps.storage.get(credentialId);
        if (!credential) {
            throw new Error(`Credential ${credentialId} not found in storage`);
        }
        const keyData = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildKeyData"])(credential.publicKey, credentialId);
        const signer = {
            tag: "External",
            values: [
                this.deps.webauthnVerifierAddress,
                keyData
            ]
        };
        return this.remove(contextRuleId, signer);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/context-rule-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Context Rule Manager
 *
 * Manages context rules (authorization policies) for smart accounts.
 * Context rules define which signers and policies apply to specific
 * operation types (default, contract calls, contract creation).
 */ /**
 * Manages context rules for smart accounts.
 *
 * Context rules are the core authorization mechanism for smart accounts.
 * Each rule specifies:
 * - A context type (when the rule applies)
 * - A set of signers (who can authorize)
 * - A set of policies (additional constraints)
 * - An optional expiration
 *
 * @example
 * ```typescript
 * // Add a new context rule
 * const tx = await kit.rules.add(
 *   { tag: "Default", values: undefined },
 *   "My Rule",
 *   [passkeySigner, delegatedSigner],
 *   new Map([[policyAddress, thresholdParams]])
 * );
 * await tx.signAndSend();
 * ```
 */ __turbopack_context__.s([
    "ContextRuleManager",
    ()=>ContextRuleManager
]);
class ContextRuleManager {
    deps;
    constructor(deps){
        this.deps = deps;
    }
    /**
     * Add a new context rule to the smart account.
     *
     * @param contextType - When this rule applies (Default, CallContract, CreateContract)
     * @param name - Human-readable name for the rule
     * @param signers - Array of signers that can authorize under this rule
     * @param policies - Map of policy addresses to their parameters
     * @param validUntil - Optional ledger number when this rule expires
     * @returns Assembled transaction that creates the rule when signed and sent
     * @throws Error if not connected to a wallet
     */ async add(contextType, name, signers, policies, validUntil) {
        return this.deps.requireWallet().wallet.add_context_rule({
            context_type: contextType,
            name,
            valid_until: validUntil,
            signers,
            policies
        });
    }
    /**
     * Get a context rule by its ID.
     *
     * @param contextRuleId - The numeric ID of the rule to retrieve
     * @returns Assembled transaction that returns the rule (or undefined if not found)
     * @throws Error if not connected to a wallet
     */ async get(contextRuleId) {
        return this.deps.requireWallet().wallet.get_context_rule({
            context_rule_id: contextRuleId
        });
    }
    /**
     * Get all context rules of a specific type.
     *
     * @param contextRuleType - The type of rules to retrieve (Default, CallContract, CreateContract)
     * @returns Assembled transaction that returns an array of matching rules
     * @throws Error if not connected to a wallet
     */ async getAll(contextRuleType) {
        return this.deps.requireWallet().wallet.get_context_rules({
            context_rule_type: contextRuleType
        });
    }
    /**
     * Remove a context rule from the smart account.
     *
     * @param contextRuleId - The numeric ID of the rule to remove
     * @returns Assembled transaction that removes the rule when signed and sent
     * @throws Error if not connected to a wallet
     */ async remove(contextRuleId) {
        return this.deps.requireWallet().wallet.remove_context_rule({
            context_rule_id: contextRuleId
        });
    }
    /**
     * Update the name of a context rule.
     *
     * @param contextRuleId - The numeric ID of the rule to update
     * @param name - The new name for the rule
     * @returns Assembled transaction that updates the rule when signed and sent
     * @throws Error if not connected to a wallet
     */ async updateName(contextRuleId, name) {
        return this.deps.requireWallet().wallet.update_context_rule_name({
            context_rule_id: contextRuleId,
            name
        });
    }
    /**
     * Update the expiration of a context rule.
     *
     * @param contextRuleId - The numeric ID of the rule to update
     * @param validUntil - The new expiration ledger number (undefined for no expiration)
     * @returns Assembled transaction that updates the rule when signed and sent
     * @throws Error if not connected to a wallet
     */ async updateExpiration(contextRuleId, validUntil) {
        return this.deps.requireWallet().wallet.update_context_rule_valid_until({
            context_rule_id: contextRuleId,
            valid_until: validUntil
        });
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/policy-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Policy Manager
 *
 * Manages policies attached to context rules.
 * Policies provide additional authorization constraints beyond signers.
 */ /**
 * Manages policies for smart account context rules.
 *
 * Policies are contracts that enforce additional authorization constraints.
 * Common policy types include:
 * - Threshold policies (require N-of-M signers)
 * - Weighted threshold policies (signers have different weights)
 * - Spending limit policies (restrict transaction amounts)
 *
 * @example
 * ```typescript
 * import { createThresholdParams } from "smart-account-kit";
 *
 * // Add a 2-of-3 threshold policy
 * const params = createThresholdParams(2);
 * const tx = await kit.policies.add(
 *   contextRuleId,
 *   thresholdPolicyAddress,
 *   params
 * );
 * await tx.signAndSend();
 * ```
 */ __turbopack_context__.s([
    "PolicyManager",
    ()=>PolicyManager
]);
class PolicyManager {
    deps;
    constructor(deps){
        this.deps = deps;
    }
    /**
     * Add a policy to a context rule.
     *
     * @param contextRuleId - The numeric ID of the context rule to add the policy to
     * @param policyAddress - The contract address of the policy to add
     * @param installParams - Policy-specific installation parameters
     * @returns Assembled transaction that adds the policy when signed and sent
     * @throws Error if not connected to a wallet
     */ async add(contextRuleId, policyAddress, installParams) {
        return this.deps.requireWallet().wallet.add_policy({
            context_rule_id: contextRuleId,
            policy: policyAddress,
            install_param: installParams
        });
    }
    /**
     * Remove a policy from a context rule.
     *
     * @param contextRuleId - The numeric ID of the context rule to remove the policy from
     * @param policyAddress - The contract address of the policy to remove
     * @returns Assembled transaction that removes the policy when signed and sent
     * @throws Error if not connected to a wallet
     */ async remove(contextRuleId, policyAddress) {
        return this.deps.requireWallet().wallet.remove_policy({
            context_rule_id: contextRuleId,
            policy: policyAddress
        });
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/credential-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CredentialManager",
    ()=>CredentialManager
]);
/**
 * Credential Manager
 *
 * Manages WebAuthn credentials (passkeys) including creation, storage,
 * deployment, and synchronization with on-chain state.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
;
;
class CredentialManager {
    deps;
    constructor(deps){
        this.deps = deps;
    }
    /**
     * Get all stored credentials.
     */ async getAll() {
        return this.deps.storage.getAll();
    }
    /**
     * Get credentials for the current wallet.
     */ async getForWallet() {
        const contractId = this.deps.getContractId();
        if (!contractId) {
            return [];
        }
        return this.deps.storage.getByContract(contractId);
    }
    /**
     * Get credentials that are pending deployment.
     */ async getPending() {
        const all = await this.deps.storage.getAll();
        return all.filter((c)=>c.deploymentStatus === "pending" || c.deploymentStatus === "failed");
    }
    /**
     * Create a new passkey and save it to storage.
     */ async create(options) {
        const now = new Date();
        const nickname = options?.nickname || `Passkey ${now.toLocaleDateString()}`;
        const appName = options?.appName || this.deps.rpName;
        const { rawResponse, credentialId, publicKey } = await this.deps.createPasskey(appName, nickname);
        const storedCredential = {
            credentialId,
            publicKey,
            contractId: "",
            nickname,
            createdAt: Date.now(),
            transports: rawResponse?.response?.transports,
            deploymentStatus: "pending"
        };
        await this.deps.storage.save(storedCredential);
        this.deps.events.emit("credentialCreated", {
            credential: storedCredential
        });
        return storedCredential;
    }
    /**
     * Save a credential to storage.
     */ async save(credential) {
        const storedCredential = {
            credentialId: credential.credentialId,
            publicKey: credential.publicKey,
            contractId: credential.contractId || "",
            nickname: credential.nickname,
            createdAt: Date.now(),
            deploymentStatus: "pending"
        };
        await this.deps.storage.save(storedCredential);
        return storedCredential;
    }
    /**
     * Deploy a wallet using an existing pending credential.
     */ async deploy(credentialId, options) {
        const credential = await this.deps.storage.get(credentialId);
        if (!credential) {
            throw new Error(`Credential ${credentialId} not found in storage`);
        }
        const credentialIdBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId);
        const contractId = this.deps.deriveContractAddress(credentialIdBuffer);
        const deployTx = await this.deps.buildDeployTransaction(credentialIdBuffer, credential.publicKey);
        // Sign the deployment transaction with the deployer keypair
        // Deployment uses source_account auth which requires envelope signature
        // When using Relayer, the signed XDR is fee-bumped (inner signature preserved)
        const submissionOpts = {
            forceMethod: options?.forceMethod
        };
        await this.deps.signWithDeployer(deployTx);
        if (!deployTx.signed) {
            throw new Error("Failed to sign deployment transaction");
        }
        const signedTransaction = deployTx.signed.toXDR();
        this.deps.setConnectedState(contractId, credentialId);
        this.deps.initializeWallet(contractId);
        this.deps.events.emit("walletConnected", {
            contractId,
            credentialId
        });
        const submitResult = options?.autoSubmit ? await this.deps.submitDeploymentTx(deployTx, credentialId, submissionOpts) : undefined;
        return {
            contractId,
            signedTransaction,
            submitResult
        };
    }
    /**
     * Mark a credential as deployed (removes from storage).
     */ async markDeployed(credentialId) {
        await this.deps.storage.delete(credentialId);
    }
    /**
     * Mark a credential as failed.
     */ async markFailed(credentialId, error) {
        await this.deps.storage.update(credentialId, {
            deploymentStatus: "failed",
            deploymentError: error
        });
    }
    /**
     * Sync a credential with on-chain state.
     * If deployed, removes from storage. Returns true if deployed.
     */ async sync(credentialId) {
        const credential = await this.deps.storage.get(credentialId);
        if (!credential) {
            throw new Error(`Credential ${credentialId} not found in storage`);
        }
        try {
            await this.deps.rpc.getContractData(credential.contractId, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvLedgerKeyContractInstance());
            await this.deps.storage.delete(credentialId);
            return true;
        } catch  {
            return false;
        }
    }
    /**
     * Sync all stored credentials with on-chain state.
     */ async syncAll() {
        const all = await this.deps.storage.getAll();
        let deployed = 0;
        let pending = 0;
        let failed = 0;
        for (const credential of all){
            const exists = await this.sync(credential.credentialId);
            if (exists) {
                deployed++;
            } else if (credential.deploymentStatus === "failed") {
                failed++;
            } else {
                pending++;
            }
        }
        return {
            deployed,
            pending,
            failed
        };
    }
    /**
     * Delete a pending credential.
     */ async delete(credentialId) {
        const credential = await this.deps.storage.get(credentialId);
        if (!credential) {
            throw new Error(`Credential ${credentialId} not found in storage`);
        }
        const isDeployed = await this.sync(credentialId);
        if (isDeployed) {
            throw new Error("Cannot delete a deployed credential. The wallet exists on-chain.");
        }
        await this.deps.storage.delete(credentialId);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/builders.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "collectUniqueSigners",
    ()=>collectUniqueSigners,
    "createCallContractContext",
    ()=>createCallContractContext,
    "createCreateContractContext",
    ()=>createCreateContractContext,
    "createDefaultContext",
    ()=>createDefaultContext,
    "createDelegatedSigner",
    ()=>createDelegatedSigner,
    "createEd25519Signer",
    ()=>createEd25519Signer,
    "createExternalSigner",
    ()=>createExternalSigner,
    "createSpendingLimitParams",
    ()=>createSpendingLimitParams,
    "createThresholdParams",
    ()=>createThresholdParams,
    "createWebAuthnSigner",
    ()=>createWebAuthnSigner,
    "createWeightedThresholdParams",
    ()=>createWeightedThresholdParams,
    "describeSignerType",
    ()=>describeSignerType,
    "formatContextType",
    ()=>formatContextType,
    "formatSignerForDisplay",
    ()=>formatSignerForDisplay,
    "getCredentialIdFromSigner",
    ()=>getCredentialIdFromSigner,
    "getSignerKey",
    ()=>getSignerKey,
    "isDelegatedSigner",
    ()=>isDelegatedSigner,
    "isExternalSigner",
    ()=>isExternalSigner,
    "parseSigner",
    ()=>parseSigner,
    "signerMatchesAddress",
    ()=>signerMatchesAddress,
    "signerMatchesCredential",
    ()=>signerMatchesCredential,
    "signersEqual",
    ()=>signersEqual,
    "truncateAddress",
    ()=>truncateAddress
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
/**
 * Builder utilities for Smart Account Kit
 *
 * Type-safe constructors for creating signers, context rule types, and policy parameters.
 * These helpers ensure correct data structures are created for smart account operations.
 *
 * @packageDocumentation
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
;
;
function createDelegatedSigner(publicKey) {
    // Validate the address format
    if (!publicKey.startsWith("G") || publicKey.length !== 56) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Invalid Stellar account address. Must start with 'G' and be 56 characters.", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_ADDRESS, {
            publicKey
        });
    }
    return {
        tag: "Delegated",
        values: [
            publicKey
        ]
    };
}
function createExternalSigner(verifierAddress, keyData) {
    // Validate the address format
    if (!verifierAddress.startsWith("C") || verifierAddress.length !== 56) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Invalid contract address. Must start with 'C' and be 56 characters.", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_ADDRESS, {
            verifierAddress
        });
    }
    return {
        tag: "External",
        values: [
            verifierAddress,
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(keyData)
        ]
    };
}
function createWebAuthnSigner(webauthnVerifierAddress, publicKey, credentialId) {
    const keyData = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildKeyData"])(publicKey, credentialId);
    return createExternalSigner(webauthnVerifierAddress, keyData);
}
function createEd25519Signer(ed25519VerifierAddress, publicKey) {
    if (publicKey.length !== 32) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Ed25519 public key must be 32 bytes", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            actualLength: publicKey.length
        });
    }
    return createExternalSigner(ed25519VerifierAddress, publicKey);
}
function createDefaultContext() {
    return {
        tag: "Default",
        values: undefined
    };
}
function createCallContractContext(contractAddress) {
    // Validate the address format
    if (!contractAddress.startsWith("C") || contractAddress.length !== 56) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Invalid contract address. Must start with 'C' and be 56 characters.", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_ADDRESS, {
            contractAddress
        });
    }
    return {
        tag: "CallContract",
        values: [
            contractAddress
        ]
    };
}
function createCreateContractContext(wasmHash) {
    let hashBuffer;
    if (typeof wasmHash === "string") {
        // Remove 0x prefix if present
        const cleanHash = wasmHash.startsWith("0x") ? wasmHash.slice(2) : wasmHash;
        if (cleanHash.length !== 64) {
            throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("WASM hash must be 32 bytes (64 hex characters)", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
                actualLength: cleanHash.length
            });
        }
        hashBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(cleanHash, "hex");
    } else {
        if (wasmHash.length !== 32) {
            throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("WASM hash must be 32 bytes", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
                actualLength: wasmHash.length
            });
        }
        hashBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(wasmHash);
    }
    return {
        tag: "CreateContract",
        values: [
            hashBuffer
        ]
    };
}
function createThresholdParams(threshold) {
    if (threshold < 1) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Threshold must be at least 1", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            threshold
        });
    }
    if (!Number.isInteger(threshold)) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Threshold must be an integer", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            threshold
        });
    }
    return {
        threshold
    };
}
function createWeightedThresholdParams(threshold, signerWeights) {
    if (threshold < 1) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Threshold must be at least 1", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            threshold
        });
    }
    if (!Number.isInteger(threshold)) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Threshold must be an integer", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            threshold
        });
    }
    if (signerWeights.size === 0) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("At least one signer weight must be provided", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT);
    }
    // Validate all weights are positive integers and calculate sum
    let totalWeight = 0;
    for (const [, weight] of signerWeights){
        if (weight < 1 || !Number.isInteger(weight)) {
            throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("All weights must be positive integers", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
                weight
            });
        }
        totalWeight += weight;
    }
    // Validate that threshold is achievable
    if (totalWeight < threshold) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"](`Sum of weights (${totalWeight}) must be >= threshold (${threshold})`, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            threshold,
            totalWeight
        });
    }
    // Fields must be in alphabetical order for Soroban ScMap serialization
    return {
        signer_weights: signerWeights,
        threshold
    };
}
function createSpendingLimitParams(spendingLimit, periodLedgers) {
    if (periodLedgers < 1) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Period must be at least 1 ledger", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            periodLedgers
        });
    }
    if (!Number.isInteger(periodLedgers)) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Period must be an integer number of ledgers", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            periodLedgers
        });
    }
    const limitBigInt = typeof spendingLimit === "bigint" ? spendingLimit : BigInt(spendingLimit);
    if (limitBigInt < 1n) {
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Spending limit must be positive", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_AMOUNT, {
            spendingLimit: limitBigInt.toString()
        });
    }
    // Fields must be in alphabetical order for Soroban ScMap serialization
    return {
        period_ledgers: periodLedgers,
        spending_limit: limitBigInt
    };
}
;
function parseSigner(input) {
    if (typeof input === "string") {
        // Check if it's a Stellar account address
        if (input.startsWith("G") && input.length === 56) {
            return createDelegatedSigner(input);
        }
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ValidationError"]("Invalid signer input. Use a Stellar address (G...) or a Signer object.", __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountErrorCode"].INVALID_INPUT, {
            input
        });
    }
    return input;
}
function getCredentialIdFromSigner(signer) {
    if (signer.tag !== "External") {
        return null;
    }
    const keyData = signer.values[1];
    if (keyData.length <= __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]) {
        return null; // Not a WebAuthn signer (no credential ID suffix)
    }
    const credentialId = keyData.slice(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]);
    // Convert to base64url
    return credentialId.toString("base64").replace(/\+/g, "-").replace(/\//g, "_").replace(/=/g, "");
}
function isDelegatedSigner(signer) {
    return signer.tag === "Delegated";
}
function isExternalSigner(signer) {
    return signer.tag === "External";
}
function describeSignerType(signer) {
    if (signer.tag === "Delegated") {
        return "Stellar Account";
    }
    const keyData = signer.values[1];
    // WebAuthn signers have 65-byte pubkey + credential ID
    if (keyData.length > 65) {
        return "Passkey (WebAuthn)";
    }
    // Ed25519 signers have 32-byte public key
    if (keyData.length === 32) {
        return "Ed25519";
    }
    return "External Verifier";
}
function truncateAddress(address, chars = 4) {
    if (address.length <= chars * 2 + 3) {
        return address;
    }
    return `${address.slice(0, chars)}...${address.slice(-chars)}`;
}
function signerMatchesCredential(signer, credentialId) {
    const signerCredId = getCredentialIdFromSigner(signer);
    return signerCredId === credentialId;
}
function signerMatchesAddress(signer, address) {
    if (signer.tag !== "Delegated") {
        return false;
    }
    return signer.values[0] === address;
}
function formatSignerForDisplay(signer) {
    if (signer.tag === "Delegated") {
        return {
            type: "G-Address",
            display: truncateAddress(signer.values[0], 6)
        };
    }
    const credentialId = getCredentialIdFromSigner(signer);
    if (credentialId) {
        return {
            type: "Passkey",
            display: `cred:${credentialId.slice(0, 8)}...`
        };
    }
    const keyData = signer.values[1];
    if (keyData.length === 32) {
        return {
            type: "Ed25519",
            display: `key:${keyData.toString("hex").slice(0, 8)}...`
        };
    }
    return {
        type: "External",
        display: truncateAddress(signer.values[0], 4)
    };
}
function signersEqual(a, b) {
    if (a.tag !== b.tag) return false;
    if (a.tag === "Delegated" && b.tag === "Delegated") {
        return a.values[0] === b.values[0];
    }
    if (a.tag === "External" && b.tag === "External") {
        const aVerifier = a.values[0];
        const bVerifier = b.values[0];
        const aKey = a.values[1];
        const bKey = b.values[1];
        return aVerifier === bVerifier && aKey.equals(bKey);
    }
    return false;
}
function getSignerKey(signer) {
    if (signer.tag === "Delegated") {
        return `delegated:${signer.values[0]}`;
    }
    const keyData = signer.values[1];
    return `external:${signer.values[0]}:${keyData.toString("hex")}`;
}
function collectUniqueSigners(signers) {
    const signerMap = new Map();
    for (const signer of signers){
        const key = getSignerKey(signer);
        if (!signerMap.has(key)) {
            signerMap.set(key, signer);
        }
    }
    return Array.from(signerMap.values());
}
function formatContextType(contextType) {
    if (contextType.tag === "Default") {
        return "Default (Any Operation)";
    }
    if (contextType.tag === "CallContract") {
        return `Call Contract: ${truncateAddress(contextType.values[0])}`;
    }
    if (contextType.tag === "CreateContract") {
        const hashBytes = contextType.values[0];
        const hashHex = hashBytes.toString("hex");
        return `Create Contract: ${hashHex.slice(0, 8)}...`;
    }
    return "Unknown";
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/multi-signer-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MultiSignerManager",
    ()=>MultiSignerManager
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
/**
 * Multi-Signer Manager
 *
 * Handles multi-signature operations for smart accounts, coordinating
 * between passkey signers and external wallet signers.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$builders$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/builders.js [app-client] (ecmascript) <locals>");
;
const { assembleTransaction } = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["rpc"];
;
;
function isSendTransactionResult(value) {
    return typeof value === "object" && value !== null && "status" in value && typeof value.status === "string";
}
class MultiSignerManager {
    deps;
    constructor(deps){
        this.deps = deps;
    }
    /**
     * Get available signers from on-chain context rules.
     */ async getAvailableSigners() {
        if (!this.deps.isConnected()) {
            return [];
        }
        try {
            const defaultRulesResult = await this.deps.getRules({
                tag: "Default",
                values: undefined
            });
            const defaultRules = defaultRulesResult.result || [];
            // Collect all signers from all rules, then deduplicate
            const allSigners = defaultRules.flatMap((rule)=>rule.signers);
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$builders$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["collectUniqueSigners"])(allSigners);
        } catch (error) {
            console.warn("[SmartAccountKit] Failed to fetch available signers:", error);
            return [];
        }
    }
    /**
     * Extract credential ID from a signer.
     */ extractCredentialId(signer) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$builders$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getCredentialIdFromSigner"])(signer);
    }
    /**
     * Check if a signer matches a credential ID.
     */ signerMatchesCredential(signer, credentialId) {
        const signerCredId = this.extractCredentialId(signer);
        return signerCredId === credentialId;
    }
    /**
     * Check if a signer matches a wallet address.
     */ signerMatchesAddress(signer, address) {
        if (signer.tag !== "Delegated") return false;
        return signer.values[0] === address;
    }
    /**
     * Check if an operation needs multi-signer handling.
     */ needsMultiSigner(signers) {
        // Check for delegated signers
        const hasDelegated = signers.some((s)=>s.tag === "Delegated");
        // Check for multiple signers
        const hasMultiple = signers.length > 1;
        return hasDelegated || hasMultiple;
    }
    /**
     * Build selected signers from available signers.
     */ buildSelectedSigners(signers, activeCredentialId) {
        const selected = [];
        for (const signer of signers){
            if (signer.tag === "Delegated") {
                const address = signer.values[0];
                if (this.deps.externalSigners.canSignFor(address)) {
                    selected.push({
                        signer,
                        type: "wallet",
                        walletAddress: address
                    });
                }
            } else {
                const credId = this.extractCredentialId(signer);
                if (credId && (!activeCredentialId || credId === activeCredentialId)) {
                    selected.push({
                        signer,
                        type: "passkey",
                        credentialId: credId
                    });
                }
            }
        }
        return selected;
    }
    /**
     * Execute a generic smart account operation with multiple signers.
     */ async operation(assembledTx, selectedSigners, options) {
        const onLog = options?.onLog ?? (()=>{});
        const contractId = this.deps.getContractId();
        if (!contractId) {
            return {
                success: false,
                hash: "",
                error: "Not connected to a wallet"
            };
        }
        const passkeySigners = selectedSigners.filter((s)=>s.type === "passkey");
        const walletSigners = selectedSigners.filter((s)=>s.type === "wallet");
        onLog(`Signing with ${passkeySigners.length} passkey(s) and ${walletSigners.length} wallet(s)`);
        // Validate wallet signers
        for (const walletSigner of walletSigners){
            if (!walletSigner.walletAddress) continue;
            if (!this.deps.externalSigners.canSignFor(walletSigner.walletAddress)) {
                return {
                    success: false,
                    hash: "",
                    error: `No signer available for address: ${walletSigner.walletAddress}. ` + `Use kit.externalSigners.addFromSecret() or kit.externalSigners.addFromWallet() to add a signer.`
                };
            }
        }
        try {
            const builtTx = assembledTx.built;
            if (!builtTx) {
                return {
                    success: false,
                    hash: "",
                    error: "Transaction not built"
                };
            }
            const ops = builtTx.operations;
            if (!ops || ops.length === 0) {
                return {
                    success: false,
                    hash: "",
                    error: "No operations in transaction"
                };
            }
            const invokeOp = ops[0];
            const authEntries = invokeOp.auth || [];
            onLog(`Found ${authEntries.length} auth entries to sign`);
            if (authEntries.length === 0) {
                onLog("No auth entries - submitting directly...");
                const result = await assembledTx.signAndSend();
                if (!isSendTransactionResult(result)) {
                    return {
                        success: false,
                        hash: "",
                        error: "Unexpected transaction result format"
                    };
                }
                return {
                    success: result.status === "SUCCESS",
                    hash: result.hash || "",
                    error: result.status !== "SUCCESS" ? "Transaction failed" : undefined
                };
            }
            const signedAuthEntries = [];
            const { sequence } = await this.deps.rpc.getLatestLedger();
            const expiration = sequence + __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["AUTH_ENTRY_EXPIRATION_BUFFER"];
            for (const entry of authEntries){
                const credentials = entry.credentials();
                if (credentials.switch().name !== "sorobanCredentialsAddress") {
                    signedAuthEntries.push(entry);
                    continue;
                }
                const addressCreds = credentials.address();
                const authAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromScAddress(addressCreds.address()).toString();
                if (authAddress === contractId) {
                    let signedEntry = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry.fromXDR(entry.toXDR());
                    signedEntry.credentials().address().signatureExpirationLedger(expiration);
                    // Sign with passkeys
                    for(let i = 0; i < passkeySigners.length; i++){
                        const passkeySigner = passkeySigners[i];
                        onLog(`Signing with passkey ${i + 1}/${passkeySigners.length}...`);
                        signedEntry = await this.deps.signAuthEntry(signedEntry, {
                            credentialId: passkeySigner?.credentialId,
                            expiration
                        });
                    }
                    // Add delegated signers to signature map
                    for (const walletSigner of walletSigners){
                        if (!walletSigner.walletAddress) continue;
                        const delegatedSignerKey = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("Delegated"),
                            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress())
                        ]);
                        const ourSig = signedEntry.credentials().address().signature();
                        const emptyBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].alloc(0));
                        if (ourSig.switch().name === "scvVoid") {
                            signedEntry.credentials().address().signature(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                                    new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                        key: delegatedSignerKey,
                                        val: emptyBytes
                                    })
                                ])
                            ]));
                        } else {
                            const sigMap = ourSig.vec()?.[0].map();
                            if (sigMap) {
                                sigMap.push(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                    key: delegatedSignerKey,
                                    val: emptyBytes
                                }));
                                sigMap.sort((a, b)=>a.key().toXDR("hex").localeCompare(b.key().toXDR("hex")));
                            }
                        }
                    }
                    signedAuthEntries.push(signedEntry);
                    // Create delegated signer auth entries
                    if (walletSigners.length > 0) {
                        onLog(`Creating auth entries for ${walletSigners.length} delegated signer(s)...`);
                        const smartAccountPreimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                            networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(this.deps.networkPassphrase)),
                            nonce: signedEntry.credentials().address().nonce(),
                            signatureExpirationLedger: expiration,
                            invocation: signedEntry.rootInvocation()
                        }));
                        const signaturePayload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(smartAccountPreimage.toXDR());
                        for (const walletSigner of walletSigners){
                            if (!walletSigner.walletAddress) continue;
                            onLog(`Getting delegated auth from ${walletSigner.walletAddress.slice(0, 8)}...`);
                            const delegatedNonce = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString(Date.now().toString());
                            const delegatedInvocation = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizedInvocation({
                                function: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizedFunction.sorobanAuthorizedFunctionTypeContractFn(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].InvokeContractArgs({
                                    contractAddress: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(contractId).toScAddress(),
                                    functionName: "__check_auth",
                                    args: [
                                        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signaturePayload)
                                    ]
                                })),
                                subInvocations: []
                            });
                            const delegatedPreimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                                networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(this.deps.networkPassphrase)),
                                nonce: delegatedNonce,
                                signatureExpirationLedger: expiration,
                                invocation: delegatedInvocation
                            }));
                            const { signedAuthEntry: walletSignatureBase64 } = await this.deps.externalSigners.signAuthEntry(delegatedPreimage.toXDR("base64"), walletSigner.walletAddress);
                            const signatureBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(walletSignatureBase64, "base64");
                            const walletPublicKeyBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress().accountId().ed25519();
                            const signatureScVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                                    new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                        key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("public_key"),
                                        val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(walletPublicKeyBytes)
                                    }),
                                    new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                        key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
                                        val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signatureBytes)
                                    })
                                ])
                            ]);
                            const walletSignedEntry = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry({
                                credentials: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanCredentials.sorobanCredentialsAddress(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAddressCredentials({
                                    address: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress(),
                                    nonce: delegatedNonce,
                                    signatureExpirationLedger: expiration,
                                    signature: signatureScVal
                                })),
                                rootInvocation: delegatedInvocation
                            });
                            signedAuthEntries.push(walletSignedEntry);
                        }
                    }
                } else {
                    signedAuthEntries.push(entry);
                }
            }
            // Re-simulate with signed auth entries
            onLog("Re-simulating with signatures...");
            const freshSourceAccount = await this.deps.rpc.getAccount(this.deps.deployerPublicKey);
            const hostFunc = ops[0].func;
            const resimTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](freshSourceAccount, {
                fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
                networkPassphrase: this.deps.networkPassphrase
            }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
                func: hostFunc,
                auth: signedAuthEntries
            })).setTimeout(this.deps.timeoutInSeconds).build();
            const resimResult = await this.deps.rpc.simulateTransaction(resimTx);
            if ("error" in resimResult) {
                return {
                    success: false,
                    hash: "",
                    error: `Re-simulation failed: ${resimResult.error}`
                };
            }
            const resimTxXdr = resimTx.toXDR();
            const normalizedTx = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"].fromXDR(resimTxXdr, this.deps.networkPassphrase);
            const assembled = assembleTransaction(normalizedTx, resimResult);
            const preparedTx = assembled.build();
            // Sign with deployer keypair if not using fee sponsoring, or if tx has source_account auth
            // (source_account auth requires envelope signature; Address auth has signature in entry)
            if (!this.deps.shouldUseFeeSponsoring() || this.deps.hasSourceAccountAuth(preparedTx)) {
                preparedTx.sign(this.deps.deployerKeypair);
            }
            onLog("Submitting transaction...");
            return this.deps.sendAndPoll(preparedTx);
        } catch (err) {
            return {
                success: false,
                hash: "",
                error: err instanceof Error ? err.message : "Unknown error"
            };
        }
    }
    /**
     * Execute a transfer with multiple signers.
     * Delegates to the kit's implementation which handles the complex XDR building.
     */ async transfer(tokenContract, recipient, amount, selectedSigners, options) {
        return this.deps.executeTransfer(tokenContract, recipient, amount, selectedSigners, options);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/managers/index.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
/**
 * Manager classes for SmartAccountKit
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/signer-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$context$2d$rule$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/context-rule-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$policy$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/policy-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$credential$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/credential-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$multi$2d$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/multi-signer-manager.js [app-client] (ecmascript)");
;
;
;
;
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/indexer-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "discoverContractsByAddress",
    ()=>discoverContractsByAddress,
    "discoverContractsByCredential",
    ()=>discoverContractsByCredential,
    "getContractDetailsFromIndexer",
    ()=>getContractDetailsFromIndexer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
;
async function discoverContractsByCredential(indexer, credentialId) {
    if (!indexer) return null;
    const hexCredentialId = normalizeCredentialIdToHex(credentialId);
    const result = await indexer.lookupByCredentialId(hexCredentialId);
    return result.contracts;
}
async function discoverContractsByAddress(indexer, address) {
    if (!indexer) return null;
    const result = await indexer.lookupByAddress(address);
    return result.contracts;
}
async function getContractDetailsFromIndexer(indexer, contractId) {
    if (!indexer) return null;
    return indexer.getContractDetails(contractId);
}
function normalizeCredentialIdToHex(credentialId) {
    if (/^[0-9a-fA-F]+$/.test(credentialId)) {
        return credentialId.toLowerCase();
    }
    try {
        const bytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId);
        return bytes.toString("hex");
    } catch  {
        return credentialId.toLowerCase();
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/webauthn-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "authenticatePasskey",
    ()=>authenticatePasskey,
    "createPasskey",
    ()=>createPasskey,
    "signAuthEntry",
    ()=>signAuthEntry
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
;
;
;
;
async function createPasskey(deps, appName, userName, authenticatorSelection) {
    const now = new Date();
    const displayName = `${userName} — ${now.toLocaleString()}`;
    const options = {
        challenge: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["generateChallenge"])(),
        rp: {
            id: deps.rpId,
            name: appName || deps.rpName
        },
        user: {
            id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"])(`${userName}:${now.getTime()}:${Math.random()}`),
            name: displayName,
            displayName
        },
        authenticatorSelection: {
            residentKey: authenticatorSelection?.residentKey ?? "preferred",
            userVerification: authenticatorSelection?.userVerification ?? "preferred",
            authenticatorAttachment: authenticatorSelection?.authenticatorAttachment
        },
        pubKeyCredParams: [
            {
                alg: -7,
                type: "public-key"
            }
        ],
        timeout: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["WEBAUTHN_TIMEOUT_MS"]
    };
    const rawResponse = await deps.webAuthn.startRegistration({
        optionsJSON: options
    });
    const publicKey = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["extractPublicKeyFromAttestation"])(rawResponse.response);
    return {
        rawResponse,
        credentialId: rawResponse.id,
        publicKey
    };
}
async function authenticatePasskey(deps) {
    const authOptions = {
        challenge: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["generateChallenge"])(),
        rpId: deps.rpId,
        userVerification: "preferred",
        timeout: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["WEBAUTHN_TIMEOUT_MS"]
    };
    const rawResponse = await deps.webAuthn.startAuthentication({
        optionsJSON: authOptions
    });
    return {
        credentialId: rawResponse.id,
        rawResponse
    };
}
async function signAuthEntry(deps, entry, options) {
    const entryXdrBytes = entry.toXDR();
    const normalizedEntry = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry.fromXDR(entryXdrBytes);
    const credentials = normalizedEntry.credentials().address();
    const expiration = options?.expiration ?? await deps.calculateExpiration();
    credentials.signatureExpirationLedger(expiration);
    const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
        networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
        nonce: credentials.nonce(),
        signatureExpirationLedger: credentials.signatureExpirationLedger(),
        invocation: normalizedEntry.rootInvocation()
    }));
    const payload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(preimage.toXDR());
    const credentialId = options?.credentialId ?? deps.getCredentialId();
    const authOptions = {
        challenge: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"])(payload),
        rpId: deps.rpId,
        userVerification: "preferred",
        timeout: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["WEBAUTHN_TIMEOUT_MS"],
        ...credentialId && {
            allowCredentials: [
                {
                    id: credentialId,
                    type: "public-key"
                }
            ]
        }
    };
    const authResponse = await deps.webAuthn.startAuthentication({
        optionsJSON: authOptions
    });
    const rawSignature = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(authResponse.response.signature);
    const compactedSignature = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["compactSignature"])(rawSignature);
    const credentialIdBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(authResponse.id);
    const contextRuleTypes = buildContextRuleTypes(normalizedEntry);
    const keyData = await findKeyDataByCredentialId(deps.requireWallet, credentialIdBuffer, contextRuleTypes);
    const signerId = {
        tag: "External",
        values: [
            deps.webauthnVerifierAddress,
            keyData
        ]
    };
    const webAuthnSigData = {
        authenticator_data: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(authResponse.response.authenticatorData),
        client_data: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(authResponse.response.clientDataJSON),
        signature: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(compactedSignature)
    };
    const scMapEntry = buildSignatureMapEntry(signerId, webAuthnSigData);
    const currentSig = credentials.signature();
    if (currentSig.switch().name === "scvVoid") {
        credentials.signature(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                scMapEntry
            ])
        ]));
    } else {
        currentSig.vec()?.[0].map()?.push(scMapEntry);
    }
    const sigMap = credentials.signature().vec()?.[0].map();
    if (sigMap && sigMap.length > 1) {
        sigMap.sort((a, b)=>{
            const aKeyXdr = a.key().toXDR("hex");
            const bKeyXdr = b.key().toXDR("hex");
            return aKeyXdr.localeCompare(bKeyXdr);
        });
    }
    if (credentialId) {
        await deps.storage.update(credentialId, {
            lastUsedAt: Date.now()
        });
    }
    return normalizedEntry;
}
async function findKeyDataByCredentialId(requireWallet, credentialId, contextRuleTypes) {
    const { wallet } = requireWallet();
    for (const contextRuleType of contextRuleTypes){
        const rulesResult = await wallet.get_context_rules({
            context_rule_type: contextRuleType
        });
        const rules = rulesResult.result;
        for (const rule of rules){
            for (const signer of rule.signers){
                if (signer.tag === "External") {
                    const keyData = signer.values[1];
                    if (keyData.length > __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]) {
                        const suffix = keyData.slice(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SECP256R1_PUBLIC_KEY_SIZE"]);
                        if (suffix.equals(credentialId)) {
                            return keyData;
                        }
                    }
                }
            }
        }
    }
    throw new Error(`No signer found for credential ID: ${credentialId.toString("base64")}`);
}
function buildContextRuleTypes(entry) {
    const types = [];
    const seen = new Set();
    const add = (type)=>{
        let key;
        if (type.tag === "Default") {
            key = "Default";
        } else if (type.tag === "CallContract") {
            key = `CallContract:${type.values[0]}`;
        } else {
            const wasm = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(type.values[0]);
            key = `CreateContract:${wasm.toString("hex")}`;
        }
        if (!seen.has(key)) {
            seen.add(key);
            types.push(type);
        }
    };
    const walk = (invocation)=>{
        const fn = invocation.function();
        const switchName = fn.switch().name;
        if (switchName === "sorobanAuthorizedFunctionTypeContractFn") {
            const args = fn.contractFn();
            const contractAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromScAddress(args.contractAddress()).toString();
            add({
                tag: "CallContract",
                values: [
                    contractAddress
                ]
            });
        } else if (switchName.startsWith("sorobanAuthorizedFunctionTypeCreateContract")) {
            const wasmHash = extractCreateContractWasmHash(fn);
            if (wasmHash) {
                add({
                    tag: "CreateContract",
                    values: [
                        wasmHash
                    ]
                });
            }
        }
        for (const sub of invocation.subInvocations()){
            walk(sub);
        }
    };
    walk(entry.rootInvocation());
    add({
        tag: "Default",
        values: undefined
    });
    return types;
}
function extractCreateContractWasmHash(fn) {
    const candidates = [];
    const fnAny = fn;
    if (typeof fnAny.createContractHostFn === "function") {
        candidates.push(fnAny.createContractHostFn());
    }
    if (typeof fnAny.createContractWithCtorHostFn === "function") {
        candidates.push(fnAny.createContractWithCtorHostFn());
    }
    if (typeof fnAny.createContractWithConstructorHostFn === "function") {
        candidates.push(fnAny.createContractWithConstructorHostFn());
    }
    for (const candidate of candidates){
        if (!candidate || typeof candidate !== "object") continue;
        const ctx = candidate;
        const executable = typeof ctx.executable === "function" ? ctx.executable() : ctx.executable;
        if (!executable || typeof executable !== "object") continue;
        const execAny = executable;
        const execSwitch = execAny.switch?.();
        if (execSwitch && execSwitch.name === "contractExecutableWasm") {
            const wasm = typeof execAny.wasm === "function" ? execAny.wasm() : execAny.wasm;
            if (wasm) {
                return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(wasm);
            }
        }
    }
    return null;
}
function buildSignatureMapEntry(signerId, sigData) {
    let keyVal;
    if (signerId.tag === "Delegated") {
        keyVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("Delegated"),
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(signerId.values[0]).toScAddress())
        ]);
    } else {
        keyVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("External"),
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(signerId.values[0]).toScAddress()),
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signerId.values[1])
        ]);
    }
    const sigDataScVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
        new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
            key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("authenticator_data"),
            val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(sigData.authenticator_data)
        }),
        new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
            key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("client_data"),
            val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(sigData.client_data)
        }),
        new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
            key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
            val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(sigData.signature)
        })
    ]);
    const sigDataXdrBytes = sigDataScVal.toXDR();
    const sigVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(sigDataXdrBytes);
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
        key: keyVal,
        val: sigVal
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/wallet-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "connectWallet",
    ()=>connectWallet,
    "connectWithCredentials",
    ()=>connectWithCredentials,
    "createWallet",
    ()=>createWallet,
    "disconnect",
    ()=>disconnect
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/base64url/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
;
;
;
;
async function createWallet(deps, appName, userName, options) {
    const { rawResponse, credentialId, publicKey } = await deps.createPasskey(appName, userName, options?.authenticatorSelection);
    const storedCredential = {
        credentialId,
        publicKey,
        contractId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deriveContractAddress"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId), deps.deployerKeypair.publicKey(), deps.networkPassphrase),
        nickname: options?.nickname ?? `${userName} - ${new Date().toLocaleDateString()}`,
        createdAt: Date.now(),
        transports: rawResponse?.response?.transports,
        isPrimary: true,
        deploymentStatus: "pending"
    };
    await deps.storage.save(storedCredential);
    deps.events.emit("credentialCreated", {
        credential: storedCredential
    });
    const credentialIdBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId);
    const contractId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deriveContractAddress"])(credentialIdBuffer, deps.deployerKeypair.publicKey(), deps.networkPassphrase);
    const deployTx = await deps.buildDeployTransaction(credentialIdBuffer, publicKey);
    const submissionOpts = {
        forceMethod: options?.forceMethod
    };
    await deps.signWithDeployer(deployTx);
    if (!deployTx.signed) {
        throw new Error("Failed to sign deployment transaction");
    }
    const signedTransaction = deployTx.signed.toXDR();
    deps.setConnectedState(contractId, credentialId);
    deps.events.emit("walletConnected", {
        contractId,
        credentialId
    });
    const now = Date.now();
    await deps.storage.saveSession({
        contractId,
        credentialId,
        connectedAt: now,
        expiresAt: now + (deps.sessionExpiryMs ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_SESSION_EXPIRY_MS"])
    });
    const submitResult = options?.autoSubmit ? await deps.submitDeploymentTx(deployTx, credentialId, submissionOpts) : undefined;
    let fundResult;
    if (options?.autoFund && submitResult?.success) {
        if (!options.nativeTokenContract) {
            fundResult = {
                success: false,
                hash: "",
                error: "nativeTokenContract is required for autoFund"
            };
        } else {
            fundResult = await deps.fundWallet(options.nativeTokenContract, {
                forceMethod: options?.forceMethod
            });
        }
    }
    return {
        rawResponse,
        credentialId,
        publicKey,
        contractId,
        signedTransaction,
        submitResult,
        fundResult
    };
}
async function connectWallet(deps, options) {
    let credentialId = options?.credentialId;
    let contractId = options?.contractId;
    let rawResponse;
    if (credentialId || contractId) {
        return deps.connectWithCredentials(credentialId, contractId);
    }
    if (!options?.fresh) {
        const session = await deps.storage.getSession();
        if (session) {
            if (session.expiresAt && Date.now() > session.expiresAt) {
                deps.events.emit("sessionExpired", {
                    contractId: session.contractId,
                    credentialId: session.credentialId
                });
                await deps.storage.clearSession();
            } else {
                return deps.connectWithCredentials(session.credentialId, session.contractId);
            }
        }
    }
    if (!options?.prompt && !options?.fresh) {
        return null;
    }
    const authOptions = {
        challenge: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["generateChallenge"])(),
        rpId: deps.rpId,
        userVerification: "preferred",
        timeout: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["WEBAUTHN_TIMEOUT_MS"]
    };
    rawResponse = await deps.webAuthn.startAuthentication({
        optionsJSON: authOptions
    });
    credentialId = rawResponse.id;
    const result = await deps.connectWithCredentials(credentialId);
    return {
        ...result,
        rawResponse
    };
}
async function connectWithCredentials(deps, credentialId, contractId) {
    let credential = null;
    if (credentialId) {
        credential = await deps.storage.get(credentialId);
        if (credential) {
            contractId = credential.contractId;
        }
    }
    if (!contractId && credentialId) {
        const credentialIdBuffer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$base64url$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].toBuffer(credentialId);
        contractId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deriveContractAddress"])(credentialIdBuffer, deps.deployerKeypair.publicKey(), deps.networkPassphrase);
    }
    if (!contractId) {
        throw new Error("Could not determine contract ID");
    }
    if (!credentialId) {
        throw new Error("Could not determine credential ID");
    }
    try {
        await deps.rpc.getContractData(contractId, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvLedgerKeyContractInstance());
    } catch  {
        if (credential && credential.deploymentStatus !== "failed") {
            await deps.storage.update(credentialId, {
                deploymentStatus: "pending"
            });
        }
        throw new Error(`Smart account contract not found on-chain for credential ${credentialId}. ` + "The wallet may not have been deployed yet.");
    }
    if (credential) {
        await deps.storage.delete(credentialId);
    }
    deps.setConnectedState(contractId, credentialId);
    deps.events.emit("walletConnected", {
        contractId,
        credentialId
    });
    const now = Date.now();
    await deps.storage.saveSession({
        contractId,
        credentialId,
        connectedAt: now,
        expiresAt: now + deps.sessionExpiryMs
    });
    return {
        credentialId,
        contractId,
        credential: credential ?? undefined
    };
}
async function disconnect(deps) {
    const contractId = deps.getContractId();
    deps.clearConnectedState();
    await deps.storage.clearSession();
    if (contractId) {
        deps.events.emit("walletDisconnected", {
            contractId
        });
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/tx-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "fundWallet",
    ()=>fundWallet,
    "getSubmissionMethod",
    ()=>getSubmissionMethod,
    "hasSourceAccountAuth",
    ()=>hasSourceAccountAuth,
    "sendAndPoll",
    ()=>sendAndPoll,
    "shouldUseFeeSponsoring",
    ()=>shouldUseFeeSponsoring,
    "sign",
    ()=>sign,
    "signAndSubmit",
    ()=>signAndSubmit,
    "signResimulateAndPrepare",
    ()=>signResimulateAndPrepare,
    "simulateHostFunction",
    ()=>simulateHostFunction,
    "transfer",
    ()=>transfer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
;
;
;
;
function getSubmissionMethod(relayer, options) {
    if (options?.forceMethod) {
        return options.forceMethod;
    }
    if (relayer) {
        return "relayer";
    }
    return "rpc";
}
function shouldUseFeeSponsoring(relayer, options) {
    return getSubmissionMethod(relayer, options) === "relayer";
}
async function sendAndPoll(deps, transaction, options) {
    const method = getSubmissionMethod(deps.relayer, options);
    let hash;
    switch(method){
        case "relayer":
            {
                if (!deps.relayer) {
                    return {
                        success: false,
                        hash: "",
                        error: "Relayer is not configured"
                    };
                }
                const operations = transaction.operations;
                if (operations.length !== 1) {
                    return {
                        success: false,
                        hash: "",
                        error: "Relayer requires exactly one invokeHostFunction operation"
                    };
                }
                const op = operations[0];
                if (op.type !== "invokeHostFunction") {
                    return {
                        success: false,
                        hash: "",
                        error: "Relayer only supports invokeHostFunction operations"
                    };
                }
                const invokeOp = op;
                const funcXdr = invokeOp.func.toXDR("base64");
                const authXdrs = (invokeOp.auth ?? []).map((entry)=>entry.toXDR("base64"));
                const relayerResult = await deps.relayer.send(funcXdr, authXdrs);
                if (!relayerResult.success) {
                    return {
                        success: false,
                        hash: "",
                        error: relayerResult.error ?? "Relayer submission failed"
                    };
                }
                hash = relayerResult.hash ?? "";
                break;
            }
        case "rpc":
        default:
            {
                const sendResult = await deps.rpc.sendTransaction(transaction);
                if (sendResult.status === "ERROR") {
                    return {
                        success: false,
                        hash: sendResult.hash,
                        error: sendResult.errorResult?.toXDR("base64") ?? "Transaction submission failed"
                    };
                }
                hash = sendResult.hash;
                break;
            }
    }
    const txResult = await deps.rpc.pollTransaction(hash, {
        attempts: 10
    });
    if (txResult.status === "SUCCESS") {
        return {
            success: true,
            hash,
            ledger: txResult.ledger
        };
    }
    if (txResult.status === "FAILED") {
        return {
            success: false,
            hash,
            error: "Transaction failed on-chain"
        };
    }
    return {
        success: false,
        hash,
        error: "Transaction confirmation timed out"
    };
}
function hasSourceAccountAuth(transaction) {
    for (const op of transaction.operations){
        if (op.type !== "invokeHostFunction") continue;
        const invokeOp = op;
        if (!invokeOp.auth) continue;
        for (const entry of invokeOp.auth){
            if (entry.credentials().switch().name === "sorobanCredentialsSourceAccount") {
                return true;
            }
        }
    }
    return false;
}
async function simulateHostFunction(deps, hostFunc) {
    let sourceAccount;
    try {
        sourceAccount = await deps.rpc.getAccount(deps.deployerKeypair.publicKey());
    } catch (error) {
        throw new Error(`Simulation requires the deployer account to exist on-chain. ` + `Fund ${deps.deployerKeypair.publicKey()} before simulating transactions.`);
    }
    const simulationTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](sourceAccount, {
        fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
        networkPassphrase: deps.networkPassphrase
    }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
        func: hostFunc,
        auth: []
    })).setTimeout(deps.timeoutInSeconds).build();
    const simResult = await deps.rpc.simulateTransaction(simulationTx);
    if ("error" in simResult) {
        throw new Error(`Simulation failed: ${simResult.error}`);
    }
    return {
        authEntries: simResult.result?.auth || []
    };
}
async function signResimulateAndPrepare(deps, hostFunc, authEntries, options) {
    const signedAuthEntries = [];
    for (const authEntry of authEntries){
        const signedEntry = await deps.signAuthEntry(authEntry, {
            credentialId: options?.credentialId,
            expiration: options?.expiration
        });
        signedAuthEntries.push(signedEntry);
    }
    let sourceAccount;
    try {
        sourceAccount = await deps.rpc.getAccount(deps.deployerKeypair.publicKey());
    } catch (error) {
        throw new Error(`Re-simulation requires the deployer account to exist on-chain. ` + `Fund ${deps.deployerKeypair.publicKey()} before re-simulating transactions.`);
    }
    const resimTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](sourceAccount, {
        fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
        networkPassphrase: deps.networkPassphrase
    }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
        func: hostFunc,
        auth: signedAuthEntries
    })).setTimeout(deps.timeoutInSeconds).build();
    const resimResult = await deps.rpc.simulateTransaction(resimTx);
    if ("error" in resimResult) {
        throw new Error(`Re-simulation failed: ${resimResult.error}`);
    }
    const resimTxXdr = resimTx.toXDR();
    const normalizedTx = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"].fromXDR(resimTxXdr, deps.networkPassphrase);
    const assembled = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["rpc"].assembleTransaction(normalizedTx, resimResult);
    return assembled.build();
}
async function sign(deps, transaction, options) {
    const contractId = deps.getContractId();
    if (!contractId) {
        throw new Error("Not connected to a wallet. Call connectWallet() first.");
    }
    const credentialId = options?.credentialId ?? deps.getCredentialId();
    const expiration = options?.expiration ?? await deps.calculateExpiration();
    await transaction.signAuthEntries({
        address: contractId,
        authorizeEntry: async (entry)=>{
            const clone = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry.fromXDR(entry.toXDR());
            return deps.signAuthEntry(clone, {
                credentialId,
                expiration
            });
        }
    });
    return transaction;
}
async function signAndSubmit(deps, transaction, options) {
    if (!deps.getContractId()) {
        return {
            success: false,
            hash: "",
            error: "Not connected to a wallet. Call connectWallet() first."
        };
    }
    try {
        const builtTx = transaction.built;
        if (!builtTx) {
            return {
                success: false,
                hash: "",
                error: "Transaction has no built transaction"
            };
        }
        const operations = builtTx.operations;
        if (operations.length !== 1) {
            return {
                success: false,
                hash: "",
                error: "Expected exactly one operation"
            };
        }
        const operation = operations[0];
        if (operation.type !== "invokeHostFunction") {
            return {
                success: false,
                hash: "",
                error: "Expected invokeHostFunction operation"
            };
        }
        const invokeOp = operation;
        const simData = transaction.simulationData;
        if (!simData?.result?.auth) {
            return {
                success: false,
                hash: "",
                error: "No simulation data or auth entries"
            };
        }
        const preparedTx = await deps.signResimulateAndPrepare(invokeOp.func, simData.result.auth, {
            credentialId: options?.credentialId,
            expiration: options?.expiration
        });
        const submissionOpts = {
            forceMethod: options?.forceMethod
        };
        if (!deps.shouldUseFeeSponsoring(submissionOpts) || deps.hasSourceAccountAuth(preparedTx)) {
            preparedTx.sign(deps.deployerKeypair);
        }
        return deps.sendAndPoll(preparedTx, submissionOpts);
    } catch (err) {
        return {
            success: false,
            hash: "",
            error: err instanceof Error ? err.message : "Unknown error"
        };
    }
}
async function fundWallet(deps, nativeTokenContract, options) {
    const contractId = deps.getContractId();
    if (!contractId) {
        return {
            success: false,
            hash: "",
            error: "Not connected to a wallet"
        };
    }
    if (!deps.networkPassphrase.includes("Test")) {
        return {
            success: false,
            hash: "",
            error: "fundWallet() only works on testnet"
        };
    }
    try {
        const tempKeypair = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Keypair"].random();
        const friendbotResponse = await fetch(`${__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FRIENDBOT_URL"]}?addr=${tempKeypair.publicKey()}`);
        if (!friendbotResponse.ok) {
            const text = await friendbotResponse.text();
            return {
                success: false,
                hash: "",
                error: `Friendbot error: ${text}`
            };
        }
        const RESERVE_XLM = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FRIENDBOT_RESERVE_XLM"];
        let sourceAccount = await deps.rpc.getAccount(tempKeypair.publicKey());
        const tokenAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(nativeTokenContract);
        const fromAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(tempKeypair.publicKey());
        const balanceKey = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("Balance"),
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(fromAddress.toScAddress())
        ]);
        let balanceXlm;
        try {
            const balanceData = await deps.rpc.getContractData(nativeTokenContract, balanceKey);
            const val = balanceData.val.contractData().val();
            if (val.switch().name === "scvI128") {
                const i128 = val.i128();
                const lo = BigInt(i128.lo().toString());
                const hi = BigInt(i128.hi().toString());
                const balanceStroops = hi << BigInt(64) | lo;
                balanceXlm = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stroopsToXlm"])(balanceStroops);
            } else {
                balanceXlm = 10_000;
            }
        } catch (error) {
            console.warn("[SmartAccountKit] Failed to fetch temp account balance, using default:", error);
            balanceXlm = 10_000;
        }
        const transferAmount = balanceXlm - RESERVE_XLM;
        if (transferAmount <= 0) {
            return {
                success: false,
                hash: "",
                error: "Insufficient balance after reserve"
            };
        }
        const amountInStroops = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xlmToStroops"])(transferAmount);
        const toAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(contractId);
        const transferOp = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
            func: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HostFunction.hostFunctionTypeInvokeContract(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].InvokeContractArgs({
                contractAddress: tokenAddress.toScAddress(),
                functionName: "transfer",
                args: [
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(fromAddress.toScAddress()),
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(toAddress.toScAddress()),
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvI128(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int128Parts({
                        lo: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Uint64.fromString((amountInStroops & BigInt("0xFFFFFFFFFFFFFFFF")).toString()),
                        hi: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString((amountInStroops >> BigInt(64)).toString())
                    }))
                ]
            })),
            auth: []
        });
        const simulationTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](sourceAccount, {
            fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
            networkPassphrase: deps.networkPassphrase
        }).addOperation(transferOp).setTimeout(30).build();
        const simResult = await deps.rpc.simulateTransaction(simulationTx);
        if ("error" in simResult) {
            return {
                success: false,
                hash: "",
                error: `Simulation failed: ${simResult.error}`
            };
        }
        const authEntries = simResult.result?.auth || [];
        const signedAuthEntries = [];
        const currentLedger = simResult.latestLedger;
        const expirationLedger = currentLedger + __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LEDGERS_PER_HOUR"]; // ~1 hour
        for (const entry of authEntries){
            const credType = entry.credentials().switch().name;
            // For source_account credentials, convert to Address credentials
            // so the Relayer can use its own channel accounts
            if (credType === "sorobanCredentialsSourceAccount") {
                // Generate a nonce for the new Address credential
                const nonce = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString(Date.now().toString());
                const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                    networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
                    nonce,
                    signatureExpirationLedger: expirationLedger,
                    invocation: entry.rootInvocation()
                }));
                const payload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(preimage.toXDR());
                const signature = tempKeypair.sign(payload);
                const sigEntry = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                    key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("public_key"),
                    val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(tempKeypair.rawPublicKey())
                });
                const sigEntrySignature = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                    key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
                    val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signature)
                });
                // Create new Address credentials entry to replace source_account
                const addressEntry = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry({
                    credentials: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanCredentials.sorobanCredentialsAddress(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAddressCredentials({
                        address: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(tempKeypair.publicKey()).toScAddress(),
                        nonce,
                        signatureExpirationLedger: expirationLedger,
                        signature: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                                sigEntry,
                                sigEntrySignature
                            ])
                        ])
                    })),
                    rootInvocation: entry.rootInvocation()
                });
                signedAuthEntries.push(addressEntry);
                continue;
            }
            // For Address credentials, sign them
            if (credType === "sorobanCredentialsAddress") {
                const credentials = entry.credentials().address();
                credentials.signatureExpirationLedger(expirationLedger);
                const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                    networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
                    nonce: credentials.nonce(),
                    signatureExpirationLedger: credentials.signatureExpirationLedger(),
                    invocation: entry.rootInvocation()
                }));
                const payload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(preimage.toXDR());
                const signature = tempKeypair.sign(payload);
                const sigEntry = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                    key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("public_key"),
                    val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(tempKeypair.rawPublicKey())
                });
                const sigEntrySignature = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                    key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
                    val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signature)
                });
                credentials.signature(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                        sigEntry,
                        sigEntrySignature
                    ])
                ]));
                signedAuthEntries.push(entry);
                continue;
            }
            // Unknown credential type - push as-is (shouldn't happen)
            signedAuthEntries.push(entry);
        }
        sourceAccount = await deps.rpc.getAccount(tempKeypair.publicKey());
        const invokeHostFn = simulationTx.operations[0];
        const txWithAuth = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](sourceAccount, {
            fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
            networkPassphrase: deps.networkPassphrase
        }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
            func: invokeHostFn.func,
            auth: signedAuthEntries
        })).setTimeout(30).build();
        const txWithAuthXdr = txWithAuth.toXDR();
        const normalizedTxWithAuth = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"].fromXDR(txWithAuthXdr, deps.networkPassphrase);
        const preparedTx = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["rpc"].assembleTransaction(normalizedTxWithAuth, simResult).build();
        const submissionOpts = {
            forceMethod: options?.forceMethod
        };
        if (!deps.shouldUseFeeSponsoring(submissionOpts) || deps.hasSourceAccountAuth(preparedTx)) {
            preparedTx.sign(tempKeypair);
        }
        const txResult = await deps.sendAndPoll(preparedTx, submissionOpts);
        return {
            ...txResult,
            amount: txResult.success ? transferAmount : undefined
        };
    } catch (err) {
        return {
            success: false,
            hash: "",
            error: err instanceof Error ? err.message : "Unknown error"
        };
    }
}
async function transfer(deps, tokenContract, recipient, amount, options) {
    const contractId = deps.getContractId();
    if (!contractId) {
        return {
            success: false,
            hash: "",
            error: "Not connected to a wallet"
        };
    }
    try {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAddress"])(tokenContract, "tokenContract");
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAddress"])(recipient, "recipient");
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["validateAmount"])(amount, "amount");
    } catch (err) {
        return {
            success: false,
            hash: "",
            error: err instanceof Error ? err.message : "Validation failed"
        };
    }
    if (recipient === contractId) {
        return {
            success: false,
            hash: "",
            error: "Cannot transfer to self"
        };
    }
    try {
        const amountInStroops = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xlmToStroops"])(amount);
        const tokenAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(tokenContract);
        const fromAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(contractId);
        const toAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(recipient);
        const hostFunc = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HostFunction.hostFunctionTypeInvokeContract(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].InvokeContractArgs({
            contractAddress: tokenAddress.toScAddress(),
            functionName: "transfer",
            args: [
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(fromAddress.toScAddress()),
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(toAddress.toScAddress()),
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvI128(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int128Parts({
                    lo: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Uint64.fromString((amountInStroops & BigInt("0xFFFFFFFFFFFFFFFF")).toString()),
                    hi: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString((amountInStroops >> BigInt(64)).toString())
                }))
            ]
        }));
        const { authEntries } = await simulateHostFunction({
            rpc: deps.rpc,
            networkPassphrase: deps.networkPassphrase,
            timeoutInSeconds: deps.timeoutInSeconds,
            deployerKeypair: deps.deployerKeypair
        }, hostFunc);
        const preparedTx = await deps.signResimulateAndPrepare(hostFunc, authEntries, {
            credentialId: options?.credentialId
        });
        const submissionOpts = {
            forceMethod: options?.forceMethod
        };
        if (!deps.shouldUseFeeSponsoring(submissionOpts) || deps.hasSourceAccountAuth(preparedTx)) {
            preparedTx.sign(deps.deployerKeypair);
        }
        return deps.sendAndPoll(preparedTx, submissionOpts);
    } catch (err) {
        return {
            success: false,
            hash: "",
            error: err instanceof Error ? err.message : "Unknown error"
        };
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/deploy-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildDeployTransaction",
    ()=>buildDeployTransaction,
    "submitDeploymentTx",
    ()=>submitDeploymentTx
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2d$bindings$2f$dist$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit-bindings/dist/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/tx-ops.js [app-client] (ecmascript)");
;
;
;
;
async function submitDeploymentTx(deps, tx, credentialId, options) {
    try {
        let hashValue;
        let ledger;
        const method = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getSubmissionMethod"])(deps.relayer, options);
        if (method === "relayer" && tx.signed && deps.relayer) {
            const relayerResult = await deps.relayer.sendXdr(tx.signed);
            if (!relayerResult.success) {
                throw new Error(relayerResult.error ?? "Relayer submission failed");
            }
            hashValue = relayerResult.hash ?? "";
            const txResult = await deps.rpc.pollTransaction(hashValue, {
                attempts: 10
            });
            if (txResult.status === "SUCCESS") {
                ledger = txResult.ledger;
            } else if (txResult.status === "FAILED") {
                throw new Error("Transaction failed on-chain");
            }
        } else {
            const sentTx = await tx.send();
            const txResponse = sentTx.getTransactionResponse;
            hashValue = sentTx.sendTransactionResponse?.hash ?? "";
            ledger = txResponse?.status === "SUCCESS" ? txResponse.ledger : undefined;
        }
        await deps.storage.delete(credentialId);
        return {
            success: true,
            hash: hashValue,
            ledger
        };
    } catch (err) {
        const error = err instanceof Error ? err.message : "Transaction failed";
        await deps.storage.update(credentialId, {
            deploymentStatus: "failed",
            deploymentError: error
        });
        return {
            success: false,
            hash: "",
            error
        };
    }
}
async function buildDeployTransaction(deps, credentialId, publicKey) {
    const keyData = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildKeyData"])(publicKey, credentialId);
    const signer = {
        tag: "External",
        values: [
            deps.webauthnVerifierAddress,
            keyData
        ]
    };
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2d$bindings$2f$dist$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["Client"].deploy({
        signers: [
            signer
        ],
        policies: new Map()
    }, {
        networkPassphrase: deps.networkPassphrase,
        rpcUrl: deps.rpcUrl,
        wasmHash: deps.accountWasmHash,
        publicKey: deps.deployerKeypair.publicKey(),
        salt: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(credentialId),
        timeoutInSeconds: deps.timeoutInSeconds
    });
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/multi-signer-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "multiSignersTransfer",
    ()=>multiSignersTransfer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
;
;
async function multiSignersTransfer(deps, tokenContract, recipient, amount, selectedSigners, options) {
    const onLog = options?.onLog ?? (()=>{});
    const contractId = deps.getContractId();
    if (!contractId) {
        return {
            success: false,
            hash: "",
            error: "Not connected to a wallet"
        };
    }
    const passkeySigners = selectedSigners.filter((s)=>s.type === "passkey");
    const walletSigners = selectedSigners.filter((s)=>s.type === "wallet");
    onLog(`Signing with ${passkeySigners.length} passkey(s) and ${walletSigners.length} wallet(s)`);
    for (const walletSigner of walletSigners){
        if (!walletSigner.walletAddress) continue;
        if (!deps.externalSigners.canSignFor(walletSigner.walletAddress)) {
            return {
                success: false,
                hash: "",
                error: `No signer available for address: ${walletSigner.walletAddress}. ` + `Use kit.externalSigners.addFromSecret() or kit.externalSigners.addFromWallet() to add a signer.`
            };
        }
    }
    try {
        const amountInStroops = BigInt(Math.round(amount * __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["STROOPS_PER_XLM"]));
        const tokenAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(tokenContract);
        const fromAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(contractId);
        const toAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(recipient);
        const hostFunc = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HostFunction.hostFunctionTypeInvokeContract(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].InvokeContractArgs({
            contractAddress: tokenAddress.toScAddress(),
            functionName: "transfer",
            args: [
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(fromAddress.toScAddress()),
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(toAddress.toScAddress()),
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvI128(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int128Parts({
                    lo: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Uint64.fromString((amountInStroops & BigInt("0xFFFFFFFFFFFFFFFF")).toString()),
                    hi: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString((amountInStroops >> BigInt(64)).toString())
                }))
            ]
        }));
        onLog("Simulating transaction...");
        const sourceAccount = await deps.rpc.getAccount(deps.deployerPublicKey);
        const simulationTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](sourceAccount, {
            fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
            networkPassphrase: deps.networkPassphrase
        }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
            func: hostFunc,
            auth: []
        })).setTimeout(deps.timeoutInSeconds).build();
        const simResult = await deps.rpc.simulateTransaction(simulationTx);
        if ("error" in simResult) {
            return {
                success: false,
                hash: "",
                error: `Simulation failed: ${simResult.error}`
            };
        }
        const authEntries = simResult.result?.auth || [];
        onLog(`Found ${authEntries.length} auth entries to sign`);
        const signedAuthEntries = [];
        const { sequence } = await deps.rpc.getLatestLedger();
        const expiration = sequence + __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["AUTH_ENTRY_EXPIRATION_BUFFER"];
        for (const entry of authEntries){
            const credentials = entry.credentials();
            if (credentials.switch().name !== "sorobanCredentialsAddress") {
                signedAuthEntries.push(entry);
                continue;
            }
            const addressCreds = credentials.address();
            const authAddress = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromScAddress(addressCreds.address()).toString();
            if (authAddress === contractId) {
                let signedEntry = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry.fromXDR(entry.toXDR());
                signedEntry.credentials().address().signatureExpirationLedger(expiration);
                for(let i = 0; i < passkeySigners.length; i++){
                    const passkeySigner = passkeySigners[i];
                    onLog(`Signing smart account auth entry with passkey ${i + 1}/${passkeySigners.length}...`);
                    const credentialId = passkeySigner?.credentialId;
                    signedEntry = await deps.signAuthEntry(signedEntry, {
                        credentialId,
                        expiration
                    });
                }
                for (const walletSigner of walletSigners){
                    if (!walletSigner.walletAddress) continue;
                    const delegatedSignerKey = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("Delegated"),
                        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvAddress(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress())
                    ]);
                    const ourSig = signedEntry.credentials().address().signature();
                    const emptyBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].alloc(0));
                    if (ourSig.switch().name === "scvVoid") {
                        signedEntry.credentials().address().signature(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                                new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                    key: delegatedSignerKey,
                                    val: emptyBytes
                                })
                            ])
                        ]));
                    } else {
                        const sigMap = ourSig.vec()?.[0].map();
                        if (sigMap) {
                            sigMap.push(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                key: delegatedSignerKey,
                                val: emptyBytes
                            }));
                            sigMap.sort((a, b)=>a.key().toXDR("hex").localeCompare(b.key().toXDR("hex")));
                        }
                    }
                }
                signedAuthEntries.push(signedEntry);
                const smartAccountPreimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                    networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
                    nonce: signedEntry.credentials().address().nonce(),
                    signatureExpirationLedger: expiration,
                    invocation: signedEntry.rootInvocation()
                }));
                const signaturePayload = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(smartAccountPreimage.toXDR());
                for (const walletSigner of walletSigners){
                    if (!walletSigner.walletAddress) continue;
                    onLog(`Getting delegated auth from ${walletSigner.walletAddress.slice(0, 8)}...`);
                    const delegatedNonce = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].Int64.fromString(Date.now().toString());
                    const delegatedInvocation = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizedInvocation({
                        function: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizedFunction.sorobanAuthorizedFunctionTypeContractFn(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].InvokeContractArgs({
                            contractAddress: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(contractId).toScAddress(),
                            functionName: "__check_auth",
                            args: [
                                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signaturePayload)
                            ]
                        })),
                        subInvocations: []
                    });
                    const delegatedPreimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                        networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
                        nonce: delegatedNonce,
                        signatureExpirationLedger: expiration,
                        invocation: delegatedInvocation
                    }));
                    const delegatedPreimageXdr = delegatedPreimage.toXDR("base64");
                    const { signedAuthEntry: walletSignatureBase64 } = await deps.externalSigners.signAuthEntry(delegatedPreimageXdr, walletSigner.walletAddress);
                    const signatureBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(walletSignatureBase64, "base64");
                    const walletPublicKeyBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress().accountId().ed25519();
                    const signatureScVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                            new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("public_key"),
                                val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(walletPublicKeyBytes)
                            }),
                            new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
                                val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signatureBytes)
                            })
                        ])
                    ]);
                    const walletSignedEntry = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry({
                        credentials: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanCredentials.sorobanCredentialsAddress(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAddressCredentials({
                            address: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(walletSigner.walletAddress).toScAddress(),
                            nonce: delegatedNonce,
                            signatureExpirationLedger: expiration,
                            signature: signatureScVal
                        })),
                        rootInvocation: delegatedInvocation
                    });
                    signedAuthEntries.push(walletSignedEntry);
                }
            } else {
                const walletSigner = walletSigners.find((s)=>s.walletAddress === authAddress);
                if (walletSigner && deps.externalSigners.canSignFor(authAddress)) {
                    onLog(`Signing separate auth entry for ${authAddress.slice(0, 8)}...`);
                    const entryClone = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].SorobanAuthorizationEntry.fromXDR(entry.toXDR());
                    entryClone.credentials().address().signatureExpirationLedger(expiration);
                    const preimage = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimage.envelopeTypeSorobanAuthorization(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].HashIdPreimageSorobanAuthorization({
                        networkId: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(deps.networkPassphrase)),
                        nonce: entryClone.credentials().address().nonce(),
                        signatureExpirationLedger: expiration,
                        invocation: entryClone.rootInvocation()
                    }));
                    const preimageXdr = preimage.toXDR("base64");
                    const { signedAuthEntry: signatureBase64 } = await deps.externalSigners.signAuthEntry(preimageXdr, authAddress);
                    const signatureBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(signatureBase64, "base64");
                    const publicKeyBytes = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"].fromString(authAddress).toScAddress().accountId().ed25519();
                    const signatureScVal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVec([
                        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap([
                            new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("public_key"),
                                val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(publicKeyBytes)
                            }),
                            new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
                                key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvSymbol("signature"),
                                val: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvBytes(signatureBytes)
                            })
                        ])
                    ]);
                    entryClone.credentials().address().signature(signatureScVal);
                    signedAuthEntries.push(entryClone);
                } else {
                    onLog(`Warning: Unknown auth entry for ${authAddress}`, "error");
                    signedAuthEntries.push(entry);
                }
            }
        }
        onLog("Re-simulating with signatures...");
        const freshSourceAccount = await deps.rpc.getAccount(deps.deployerPublicKey);
        const resimTx = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"](freshSourceAccount, {
            fee: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BASE_FEE"],
            networkPassphrase: deps.networkPassphrase
        }).addOperation(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Operation"].invokeHostFunction({
            func: hostFunc,
            auth: signedAuthEntries
        })).setTimeout(deps.timeoutInSeconds).build();
        const resimResult = await deps.rpc.simulateTransaction(resimTx);
        if ("error" in resimResult) {
            return {
                success: false,
                hash: "",
                error: `Re-simulation failed: ${resimResult.error}`
            };
        }
        const resimTxXdr = resimTx.toXDR();
        const normalizedTx = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"].fromXDR(resimTxXdr, deps.networkPassphrase);
        const assembled = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["rpc"].assembleTransaction(normalizedTx, resimResult);
        const preparedTx = assembled.build();
        const submissionOpts = {
            forceMethod: options?.forceMethod
        };
        if (!deps.shouldUseFeeSponsoring(submissionOpts) || deps.hasSourceAccountAuth(preparedTx)) {
            preparedTx.sign(deps.deployerKeypair);
        }
        onLog("Submitting transaction...");
        return deps.sendAndPoll(preparedTx, submissionOpts);
    } catch (err) {
        return {
            success: false,
            hash: "",
            error: err instanceof Error ? err.message : "Unknown error"
        };
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit/policies-ops.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "buildPoliciesScVal",
    ()=>buildPoliciesScVal,
    "convertPolicyParams",
    ()=>convertPolicyParams
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
;
function convertPolicyParams(wallet, policyType, params) {
    if (!wallet) {
        return params;
    }
    const udtNames = {
        threshold: "SimpleThresholdAccountParams",
        spending_limit: "SpendingLimitAccountParams",
        weighted_threshold: "WeightedThresholdAccountParams"
    };
    const udtName = udtNames[policyType];
    if (!udtName) {
        return params;
    }
    try {
        const udtType = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScSpecTypeDef.scSpecTypeUdt(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScSpecTypeUdt({
            name: udtName
        }));
        const walletObj = wallet;
        const spec = walletObj.spec;
        if (spec && typeof spec.nativeToScVal === "function") {
            const scVal = spec.nativeToScVal(params, udtType);
            if (scVal.switch().name === "scvMap" && scVal.map()) {
                scVal.map()?.sort((a, b)=>{
                    const aKey = a.key().switch().name === "scvSymbol" ? a.key().sym().toString() : a.key().toXDR("hex");
                    const bKey = b.key().switch().name === "scvSymbol" ? b.key().sym().toString() : b.key().toXDR("hex");
                    return aKey.localeCompare(bKey);
                });
            }
            return scVal;
        }
        return params;
    } catch (error) {
        console.warn("[SmartAccountKit] Failed to convert policy params to ScVal:", error);
        return params;
    }
}
function buildPoliciesScVal(wallet, policies, policyTypes) {
    if (!wallet) {
        throw new Error("Wallet not connected");
    }
    const entries = [];
    for (const [address, params] of policies){
        const scAddress = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Address"](address).toScVal();
        const policyType = policyTypes.get(address);
        let scParams;
        if (policyType && policyType !== "custom") {
            const converted = convertPolicyParams(wallet, policyType, params);
            scParams = converted instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal ? converted : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVoid();
        } else {
            const walletObj = wallet;
            const spec = walletObj.spec;
            if (spec && typeof spec.nativeToScVal === "function") {
                try {
                    scParams = spec.nativeToScVal(params, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScSpecTypeDef.scSpecTypeVal());
                } catch  {
                    scParams = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVoid();
                }
            } else {
                scParams = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvVoid();
            }
        }
        entries.push(new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScMapEntry({
            key: scAddress,
            val: scParams
        }));
    }
    entries.sort((a, b)=>{
        const aXdr = a.key().toXDR("hex");
        const bXdr = b.key().toXDR("hex");
        return aXdr.localeCompare(bXdr);
    });
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["xdr"].ScVal.scvMap(entries);
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/kit.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "SmartAccountKit",
    ()=>SmartAccountKit
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
/**
 * SmartAccountKit - Client-side SDK for Smart Account Management
 *
 * This is the main entry point for client applications to create and manage
 * smart wallets secured by WebAuthn passkeys.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$simplewebauthn$2f$browser$2f$esm$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@simplewebauthn/browser/esm/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$simplewebauthn$2f$browser$2f$esm$2f$methods$2f$startRegistration$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@simplewebauthn/browser/esm/methods/startRegistration.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$simplewebauthn$2f$browser$2f$esm$2f$methods$2f$startAuthentication$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@simplewebauthn/browser/esm/methods/startAuthentication.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@stellar/stellar-sdk/dist/stellar-sdk.min.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$memory$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/storage/memory.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2d$bindings$2f$dist$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit-bindings/dist/index.js [app-client] (ecmascript) <locals>");
// Constants
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
// Utility functions
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
// Event emitter
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$events$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/events.js [app-client] (ecmascript)");
// External signer management
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$external$2d$signers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/external-signers.js [app-client] (ecmascript)");
// Indexer client for contract discovery
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$indexer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/indexer.js [app-client] (ecmascript)");
// Relayer client for fee-sponsored transactions via proxy
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$relayer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/relayer.js [app-client] (ecmascript)");
// Manager classes
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/signer-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$context$2d$rule$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/context-rule-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$policy$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/policy-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$credential$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/credential-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$multi$2d$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/managers/multi-signer-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$indexer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/indexer-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$webauthn$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/webauthn-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$wallet$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/wallet-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$deploy$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/deploy-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/tx-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$multi$2d$signer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/multi-signer-ops.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$policies$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit/policies-ops.js [app-client] (ecmascript)");
;
;
const { Server: RpcServer } = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["rpc"];
const { AssembledTransaction } = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["contract"];
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
class SmartAccountKit {
    // Network configuration
    rpcUrl;
    networkPassphrase;
    rpc;
    // Contract configuration
    accountWasmHash;
    webauthnVerifierAddress;
    timeoutInSeconds;
    signatureExpirationLedgers;
    // WebAuthn configuration
    rpId;
    rpName;
    webAuthn;
    // Storage
    storage;
    // External wallet adapter (optional)
    externalWalletAdapter;
    // Session configuration
    sessionExpiryMs;
    // State
    _credentialId;
    _contractId;
    /** Smart account contract client (after connection) */ wallet;
    // Deployer keypair (used as source account for contract deployment)
    deployerKeypair;
    // ==========================================================================
    // Sub-managers for organized access to contract methods
    // ==========================================================================
    /**
     * Signer management methods.
     * Add, remove, and manage signers on context rules.
     */ signers;
    /**
     * Context rule management methods.
     * Create, read, update, and delete context rules.
     */ rules;
    /**
     * Policy management methods.
     * Add and remove policies from context rules.
     */ policies;
    /**
     * Credential storage management methods.
     * Manage locally stored credentials for pending deployments.
     */ credentials;
    /**
     * Event emitter for credential lifecycle events.
     * Subscribe to events like walletConnected, credentialCreated, etc.
     *
     * @example
     * ```typescript
     * kit.events.on('walletConnected', ({ contractId }) => {
     *   console.log('Connected to wallet:', contractId);
     * });
     * ```
     */ events;
    /**
     * Multi-signer operations.
     * Execute transactions that require multiple signers (passkeys + external wallets).
     *
     * @example
     * ```typescript
     * const selectedSigners = [
     *   { type: 'passkey', credentialId: 'abc123', label: 'My Passkey' },
     *   { type: 'wallet', walletAddress: 'G...', label: 'Freighter' },
     * ];
     * const result = await kit.multiSigners.transfer(
     *   tokenContract, recipient, amount, selectedSigners
     * );
     * ```
     */ multiSigners;
    /**
     * External signer management.
     * Unified interface for managing G-address signers (Stellar accounts) for
     * multi-signature operations.
     *
     * Supports two methods of adding signers:
     * 1. Raw secret key (Keypair) - stored in memory only
     * 2. External wallet via StellarWalletsKit (if configured)
     *
     * @example
     * ```typescript
     * // Add from raw secret key (memory-only, lost on refresh)
     * const { address } = kit.externalSigners.addFromSecret("S...");
     *
     * // Add from external wallet (if SWK configured)
     * const wallet = await kit.externalSigners.addFromWallet();
     *
     * // List all external signers
     * const signers = kit.externalSigners.getAll();
     *
     * // Check if we can sign for an address
     * if (kit.externalSigners.canSignFor("G...")) {
     *   // SDK will automatically use this signer during multi-sig operations
     * }
     * ```
     */ externalSigners;
    /**
     * Indexer client for discovering smart account contracts.
     *
     * The indexer enables reverse lookups from signer credentials to contracts,
     * which is essential for discovering which contracts a user has access to.
     *
     * This is automatically configured for known networks (testnet) if not
     * explicitly disabled via `indexerUrl: false` in the config.
     *
     * @example
     * ```typescript
     * // Check if indexer is available
     * if (kit.indexer) {
     *   // Discover contracts by credential ID
     *   const { contracts } = await kit.indexer.lookupByCredentialId(credentialId);
     *
     *   // Discover contracts by G-address
     *   const { contracts } = await kit.indexer.lookupByAddress('GABCD...');
     *
     *   // Get full contract details
     *   const details = await kit.indexer.getContractDetails('CABC...');
     * }
     * ```
     */ indexer;
    /**
     * Optional Relayer client for fee-sponsored transaction submission.
     *
     * When configured, allows submitting transactions without paying fees -
     * the fees are sponsored by the Relayer proxy service.
     *
     * The Relayer uses channel accounts for parallel transaction submission with
     * automatic fee bumping, eliminating sequence number conflicts.
     *
     * @example
     * ```typescript
     * // Configure Relayer in the kit
     * const kit = new SmartAccountKit({
     *   // ... other config
     *   relayerUrl: 'https://my-relayer-proxy.example.com',
     * });
     *
     * // Submit a signed transaction via Relayer (fee-bump)
     * if (kit.relayer) {
     *   const result = await kit.relayer.sendXdr(signedTransaction);
     *   console.log('Hash:', result.hash);
     * }
     * ```
     */ relayer;
    constructor(config){
        // Validate required config
        if (!config.rpcUrl) throw new Error("rpcUrl is required");
        if (!config.networkPassphrase) throw new Error("networkPassphrase is required");
        if (!config.accountWasmHash) throw new Error("accountWasmHash is required");
        if (!config.webauthnVerifierAddress) throw new Error("webauthnVerifierAddress is required");
        // Network
        this.rpcUrl = config.rpcUrl;
        this.networkPassphrase = config.networkPassphrase;
        this.rpc = new RpcServer(config.rpcUrl);
        // Contracts
        this.accountWasmHash = config.accountWasmHash;
        this.webauthnVerifierAddress = config.webauthnVerifierAddress;
        this.timeoutInSeconds = config.timeoutInSeconds ?? 30;
        this.signatureExpirationLedgers = config.signatureExpirationLedgers ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LEDGERS_PER_HOUR"]; // ~1 hour
        // WebAuthn
        this.rpId = config.rpId;
        this.rpName = config.rpName ?? "Smart Account";
        this.webAuthn = config.webAuthn ?? {
            startRegistration: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$simplewebauthn$2f$browser$2f$esm$2f$methods$2f$startRegistration$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["startRegistration"],
            startAuthentication: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$simplewebauthn$2f$browser$2f$esm$2f$methods$2f$startAuthentication$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["startAuthentication"]
        };
        // Storage (default to memory if not provided)
        this.storage = config.storage ?? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$memory$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MemoryStorage"]();
        // External wallet adapter (optional)
        this.externalWalletAdapter = config.externalWallet;
        // Session configuration
        this.sessionExpiryMs = config.sessionExpiryMs ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_SESSION_EXPIRY_MS"];
        // Indexer client for contract discovery
        // - If indexerUrl is explicitly set to false, disable indexer
        // - If indexerUrl is a string, use that URL
        // - Otherwise, try to use default URL for the network
        if (config.indexerUrl === false) {
            this.indexer = null;
        } else if (typeof config.indexerUrl === "string") {
            this.indexer = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$indexer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IndexerClient"]({
                baseUrl: config.indexerUrl
            });
        } else {
            // Try to use default URL for this network
            const defaultUrl = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$indexer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_INDEXER_URLS"][this.networkPassphrase];
            this.indexer = defaultUrl ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$indexer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IndexerClient"]({
                baseUrl: defaultUrl
            }) : null;
        }
        // Relayer client for fee-sponsored transactions via proxy (optional)
        // Only initialize if url is provided
        this.relayer = config.relayerUrl ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$relayer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RelayerClient"](config.relayerUrl) : null;
        // Deployer keypair - deterministically derived from a fixed seed.
        // This ensures the same deployer is used across all clients.
        this.deployerKeypair = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Keypair"].fromRawEd25519Seed((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hash"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from("openzeppelin-smart-account-kit")));
        // Event emitter (initialized first as other managers may use it)
        this.events = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$events$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SmartAccountEventEmitter"]();
        // External signer manager - unified interface for G-address signers
        // Use localStorage for wallet persistence if available (browser environment)
        const walletStorage = typeof localStorage !== "undefined" ? localStorage : undefined;
        this.externalSigners = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$external$2d$signers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ExternalSignerManager"](this.networkPassphrase, this.externalWalletAdapter, walletStorage);
        // Initialize sub-managers with dependencies
        this.signers = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SignerManager"]({
            requireWallet: ()=>this.requireWallet(),
            storage: this.storage,
            events: this.events,
            webauthnVerifierAddress: this.webauthnVerifierAddress,
            createPasskey: (appName, userName)=>this.createPasskey(appName, userName)
        });
        this.rules = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$context$2d$rule$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["ContextRuleManager"]({
            requireWallet: ()=>this.requireWallet()
        });
        this.policies = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$policy$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["PolicyManager"]({
            requireWallet: ()=>this.requireWallet()
        });
        this.credentials = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$credential$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["CredentialManager"]({
            storage: this.storage,
            rpc: this.rpc,
            events: this.events,
            webauthnVerifierAddress: this.webauthnVerifierAddress,
            rpName: this.rpName,
            networkPassphrase: this.networkPassphrase,
            deployerKeypair: this.deployerKeypair,
            getContractId: ()=>this._contractId,
            setConnectedState: (contractId, credentialId)=>{
                this._contractId = contractId;
                this._credentialId = credentialId;
            },
            initializeWallet: (contractId)=>this.initializeWallet(contractId),
            createPasskey: (appName, userName)=>this.createPasskey(appName, userName),
            buildDeployTransaction: (credentialIdBuffer, publicKey)=>this.buildDeployTransaction(credentialIdBuffer, publicKey),
            signWithDeployer: (tx)=>this.signWithDeployer(tx),
            submitDeploymentTx: (tx, credentialId, options)=>this.submitDeploymentTx(tx, credentialId, options),
            deriveContractAddress: (credentialIdBuffer)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deriveContractAddress"])(credentialIdBuffer, this.deployerKeypair.publicKey(), this.networkPassphrase),
            shouldUseFeeSponsoring: (options)=>this.shouldUseFeeSponsoring(options)
        });
        this.multiSigners = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$managers$2f$multi$2d$signer$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MultiSignerManager"]({
            getContractId: ()=>this._contractId,
            isConnected: ()=>this.isConnected,
            getRules: (contextRuleType)=>this.rules.getAll(contextRuleType),
            externalSigners: this.externalSigners,
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            deployerKeypair: this.deployerKeypair,
            deployerPublicKey: this.deployerPublicKey,
            signAuthEntry: (entry, options)=>this.signAuthEntry(entry, options),
            sendAndPoll: (tx)=>this.sendAndPoll(tx),
            hasSourceAccountAuth: (tx)=>this.hasSourceAccountAuth(tx),
            executeTransfer: (tokenContract, recipient, amount, selectedSigners, options)=>this.multiSignersTransfer(tokenContract, recipient, amount, selectedSigners, options),
            shouldUseFeeSponsoring: (options)=>this.shouldUseFeeSponsoring(options)
        });
    }
    // ==========================================================================
    // Getters
    // ==========================================================================
    /** Currently connected credential ID (Base64URL encoded) */ get credentialId() {
        return this._credentialId;
    }
    /** Currently connected contract ID */ get contractId() {
        return this._contractId;
    }
    /** Check if connected to a wallet */ get isConnected() {
        return !!this._contractId;
    }
    /**
     * Get the deployer public key (used as fee payer for transactions)
     *
     * This is a deterministic keypair derived from the network passphrase,
     * shared across all SDK instances on the same network.
     */ get deployerPublicKey() {
        return this.deployerKeypair.publicKey();
    }
    // ==========================================================================
    // Contract Discovery (Indexer)
    // ==========================================================================
    /**
     * Discover smart account contracts associated with a credential ID.
     *
     * This uses the indexer to perform a reverse lookup from the credential ID
     * to find all contracts where this credential is registered as a signer.
     *
     * @param credentialId - The credential ID to look up (hex or base64url encoded)
     * @returns Array of contract summaries, or null if indexer is not available
     *
     * @example
     * ```typescript
     * // After WebAuthn authentication, find contracts for the credential
     * const contracts = await kit.discoverContractsByCredential(credentialId);
     * if (contracts && contracts.length > 0) {
     *   // User has access to these contracts
     *   console.log(`Found ${contracts.length} smart accounts`);
     * }
     * ```
     */ async discoverContractsByCredential(credentialId) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$indexer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["discoverContractsByCredential"])(this.indexer, credentialId);
    }
    /**
     * Discover smart account contracts associated with a Stellar address.
     *
     * This works for both G-addresses (Delegated signers) and C-addresses
     * (External signer verifier contracts).
     *
     * @param address - Stellar address (G... or C...)
     * @returns Array of contract summaries, or null if indexer is not available
     *
     * @example
     * ```typescript
     * // Find contracts where this G-address is a delegated signer
     * const contracts = await kit.discoverContractsByAddress('GABCD...');
     * ```
     */ async discoverContractsByAddress(address) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$indexer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["discoverContractsByAddress"])(this.indexer, address);
    }
    /**
     * Get detailed information about a smart account contract from the indexer.
     *
     * Returns the current state including active context rules, signers, and policies.
     * This is useful for displaying contract details without making on-chain calls.
     *
     * Note: For real-time data, use `kit.rules.getAll()` instead which queries on-chain.
     *
     * @param contractId - Smart account contract address (C...)
     * @returns Contract details or null if not found/indexer unavailable
     */ async getContractDetailsFromIndexer(contractId) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$indexer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getContractDetailsFromIndexer"])(this.indexer, contractId);
    }
    // ==========================================================================
    // Private Helpers - Connection Guards
    // ==========================================================================
    /**
     * Require that a wallet is connected and return the wallet client and contract ID.
     * Throws if not connected.
     * @internal
     */ requireWallet() {
        if (!this._contractId || !this.wallet) {
            throw new Error("Not connected to a wallet");
        }
        return {
            wallet: this.wallet,
            contractId: this._contractId
        };
    }
    /**
     * Initialize the wallet client for a contract.
     * @internal
     */ initializeWallet(contractId) {
        this.wallet = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2d$bindings$2f$dist$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["Client"]({
            contractId,
            networkPassphrase: this.networkPassphrase,
            rpcUrl: this.rpcUrl
        });
    }
    /**
     * Update connection state and initialize wallet client.
     * @internal
     */ setConnectedState(contractId, credentialId) {
        this._contractId = contractId;
        this._credentialId = credentialId;
        this.initializeWallet(contractId);
    }
    /**
     * Clear connection state.
     * @internal
     */ clearConnectedState() {
        this._contractId = undefined;
        this._credentialId = undefined;
        this.wallet = undefined;
    }
    /**
     * Sign an assembled transaction with the deployer keypair.
     * @internal
     */ async signWithDeployer(tx) {
        await tx.sign({
            signTransaction: async (txXdr)=>{
                const parsedTx = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$stellar$2f$stellar$2d$sdk$2f$dist$2f$stellar$2d$sdk$2e$min$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["TransactionBuilder"].fromXDR(txXdr, this.networkPassphrase);
                parsedTx.sign(this.deployerKeypair);
                return {
                    signedTxXdr: parsedTx.toXDR(),
                    signerAddress: this.deployerKeypair.publicKey()
                };
            }
        });
    }
    /**
     * Calculate expiration ledger from current ledger.
     * @internal
     */ async calculateExpiration() {
        const { sequence } = await this.rpc.getLatestLedger();
        return sequence + this.signatureExpirationLedgers;
    }
    /**
     * Submit a deployment transaction and update credential storage.
     * On success, deletes the credential from storage.
     * On failure, marks it as failed for retry.
     *
     * Deployment uses source_account auth (envelope signature). When using Relayer,
     * the signed XDR is submitted for fee-bumping. The inner tx signature is preserved.
     *
     * @internal
     */ async submitDeploymentTx(tx, credentialId, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$deploy$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["submitDeploymentTx"])({
            storage: this.storage,
            rpc: this.rpc,
            relayer: this.relayer
        }, tx, credentialId, options);
    }
    // ==========================================================================
    // Wallet Creation
    // ==========================================================================
    /**
     * Create a new smart wallet with a passkey as the primary signer
     *
     * @param appName - Application name (displayed to user during passkey creation)
     * @param userName - User identifier (displayed to user during passkey creation)
     * @param options - Additional options
     * @returns Wallet creation result with credential ID, contract ID, and signed transaction
     */ async createWallet(appName, userName, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$wallet$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createWallet"])({
            storage: this.storage,
            events: this.events,
            deployerKeypair: this.deployerKeypair,
            networkPassphrase: this.networkPassphrase,
            sessionExpiryMs: this.sessionExpiryMs,
            createPasskey: (name, user, selection)=>this.createPasskey(name, user, selection),
            buildDeployTransaction: (credentialIdBuffer, publicKey)=>this.buildDeployTransaction(credentialIdBuffer, publicKey),
            signWithDeployer: (tx)=>this.signWithDeployer(tx),
            submitDeploymentTx: (tx, credentialId, submissionOptions)=>this.submitDeploymentTx(tx, credentialId, submissionOptions),
            fundWallet: (nativeTokenContract, fundOptions)=>this.fundWallet(nativeTokenContract, fundOptions),
            setConnectedState: (contractId, credentialId)=>this.setConnectedState(contractId, credentialId)
        }, appName, userName, options);
    }
    /**
     * Create a passkey without deploying a wallet.
     * Used internally for wallet creation and adding passkey signers.
     *
     * @internal
     */ async createPasskey(appName, userName, authenticatorSelection) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$webauthn$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createPasskey"])({
            rpId: this.rpId,
            rpName: this.rpName,
            webAuthn: this.webAuthn
        }, appName, userName, authenticatorSelection);
    }
    // ==========================================================================
    // Wallet Connection
    // ==========================================================================
    /**
     * Authenticate with a passkey without connecting to a specific contract.
     *
     * This is useful when you need to:
     * 1. Get the credential ID first
     * 2. Use the indexer to discover which contracts the passkey has access to
     * 3. Then connect to a specific contract using connectWallet({ contractId, credentialId })
     *
     * @returns The credential ID from the selected passkey
     *
     * @example
     * ```typescript
     * // Step 1: Authenticate to get credential ID
     * const { credentialId } = await kit.authenticatePasskey();
     *
     * // Step 2: Discover contracts via indexer
     * const contracts = await kit.discoverContractsByCredential(credentialId);
     *
     * // Step 3: Let user choose or connect to the first one
     * if (contracts && contracts.length > 0) {
     *   await kit.connectWallet({
     *     contractId: contracts[0].contract_id,
     *     credentialId
     *   });
     * }
     * ```
     */ async authenticatePasskey() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$webauthn$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["authenticatePasskey"])({
            rpId: this.rpId,
            rpName: this.rpName,
            webAuthn: this.webAuthn
        });
    }
    /**
     * Connect to an existing smart wallet
     *
     * Behavior based on options:
     * - No options: Silent restore from storage, returns null if no stored session
     * - `{ prompt: true }`: Try stored session first, prompt user if none
     * - `{ fresh: true }`: Ignore stored session, always prompt user
     * - `{ credentialId }`: Connect using specific credential ID
     * - `{ contractId }`: Connect using specific contract ID
     *
     * @param options - Connection options
     * @returns Connection result, or null if no session and not prompting
     *
     * @example
     * ```typescript
     * // Page load - silent restore
     * const result = await kit.connectWallet();
     * if (!result) showConnectButton();
     *
     * // User clicks "Connect Wallet"
     * await kit.connectWallet({ prompt: true });
     *
     * // User clicks "Switch Wallet"
     * await kit.connectWallet({ fresh: true });
     * ```
     */ async connectWallet(options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$wallet$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["connectWallet"])({
            storage: this.storage,
            events: this.events,
            rpId: this.rpId,
            webAuthn: this.webAuthn,
            connectWithCredentials: (credentialId, contractId)=>this.connectWithCredentials(credentialId, contractId)
        }, options);
    }
    /**
     * Internal helper to connect with known credentials
     */ async connectWithCredentials(credentialId, contractId) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$wallet$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["connectWithCredentials"])({
            storage: this.storage,
            rpc: this.rpc,
            deployerKeypair: this.deployerKeypair,
            networkPassphrase: this.networkPassphrase,
            sessionExpiryMs: this.sessionExpiryMs,
            events: this.events,
            setConnectedState: (nextContractId, nextCredentialId)=>this.setConnectedState(nextContractId, nextCredentialId)
        }, credentialId, contractId);
    }
    /**
     * Disconnect from the current wallet and clear stored session
     */ async disconnect() {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$wallet$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["disconnect"])({
            storage: this.storage,
            events: this.events,
            clearConnectedState: ()=>this.clearConnectedState(),
            getContractId: ()=>this._contractId
        });
    }
    // ==========================================================================
    // Transaction Signing
    // ==========================================================================
    /**
     * Sign a transaction's auth entries with a passkey.
     *
     * **IMPORTANT**: This method only signs authorization entries. It does NOT
     * re-simulate the transaction. For WebAuthn signatures, you MUST re-simulate
     * before submission because WebAuthn signatures are much larger than the
     * placeholders used during initial simulation.
     *
     * For most use cases, prefer `signAndSubmit()` which handles the full flow:
     * sign → re-simulate → assemble → submit.
     *
     * @param transaction - AssembledTransaction to sign
     * @param options - Signing options
     * @returns The transaction with signed auth entries (NOT ready for direct submission)
     */ async sign(transaction, options) {
        const signed = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sign"])({
            getContractId: ()=>this._contractId,
            getCredentialId: ()=>this._credentialId,
            calculateExpiration: ()=>this.calculateExpiration(),
            signAuthEntry: (entry, signOptions)=>this.signAuthEntry(entry, signOptions)
        }, transaction, options);
        return signed;
    }
    /**
     * Sign and submit a transaction with proper re-simulation for WebAuthn.
     *
     * This is the recommended method for submitting transactions signed by the
     * smart account's passkey. It handles the full flow:
     * 1. Sign authorization entries with WebAuthn
     * 2. Re-simulate with signed entries (required for accurate resource costs)
     * 3. Assemble the transaction with correct fees
     * 4. Sign with fee payer and submit
     *
     * @param transaction - AssembledTransaction to sign and submit
     * @param options - Signing options
     * @returns Transaction result
     */ async signAndSubmit(transaction, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["signAndSubmit"])({
            getContractId: ()=>this._contractId,
            signResimulateAndPrepare: (hostFunc, authEntries, signOptions)=>this.signResimulateAndPrepare(hostFunc, authEntries, signOptions),
            shouldUseFeeSponsoring: (submissionOptions)=>this.shouldUseFeeSponsoring(submissionOptions),
            hasSourceAccountAuth: (preparedTx)=>this.hasSourceAccountAuth(preparedTx),
            sendAndPoll: (preparedTx, submissionOptions)=>this.sendAndPoll(preparedTx, submissionOptions),
            deployerKeypair: this.deployerKeypair
        }, transaction, options);
    }
    /**
     * Sign a single authorization entry with a passkey.
     *
     * This is a low-level method useful for multi-signer flows.
     * For most use cases, prefer:
     * - `signAndSubmit()` for full sign + re-simulate + submit flow
     * - `sign()` to sign auth entries on an AssembledTransaction
     * - `multiSigners.operation()` for multi-signer operations
     *
     * @param entry - The authorization entry to sign
     * @param options - Signing options (credentialId, expiration)
     * @returns The signed authorization entry
     */ async signAuthEntry(entry, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$webauthn$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["signAuthEntry"])({
            rpId: this.rpId,
            rpName: this.rpName,
            webAuthn: this.webAuthn,
            networkPassphrase: this.networkPassphrase,
            storage: this.storage,
            webauthnVerifierAddress: this.webauthnVerifierAddress,
            calculateExpiration: ()=>this.calculateExpiration(),
            getCredentialId: ()=>this._credentialId,
            requireWallet: ()=>this.requireWallet()
        }, entry, options);
    }
    // ==========================================================================
    // Transaction Helpers
    // ==========================================================================
    /**
     * Fund a wallet on testnet using Friendbot
     *
     * Only works on Stellar testnet. Creates a temporary account, funds it
     * via Friendbot, then transfers XLM to the smart account contract.
     * This is necessary because Friendbot can't fund contract addresses directly.
     *
     * @param nativeTokenContract - Native XLM token SAC address (required for transfer)
     * @param options - Optional settings
     * @returns Whether the funding was successful, and the amount funded
     */ async fundWallet(nativeTokenContract, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fundWallet"])({
            getContractId: ()=>this._contractId,
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            shouldUseFeeSponsoring: (submissionOptions)=>this.shouldUseFeeSponsoring(submissionOptions),
            hasSourceAccountAuth: (preparedTx)=>this.hasSourceAccountAuth(preparedTx),
            sendAndPoll: (preparedTx, submissionOptions)=>this.sendAndPoll(preparedTx, submissionOptions)
        }, nativeTokenContract, options);
    }
    /**
     * Transfer tokens from the smart wallet to a recipient
     *
     * This handles the full flow: build transaction, simulate, sign auth entries
     * with passkey, re-simulate for accurate resources, and submit.
     *
     * The deployer keypair is used as the fee payer (transaction source).
     *
     * @param tokenContract - Token contract address (SAC address for native assets)
     * @param recipient - Recipient address (G... or C...)
     * @param amount - Amount to transfer (in token units, e.g., 10 for 10 XLM)
     * @param options - Transfer options
     * @returns Transfer result
     */ async transfer(tokenContract, recipient, amount, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["transfer"])({
            getContractId: ()=>this._contractId,
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            deployerKeypair: this.deployerKeypair,
            shouldUseFeeSponsoring: (submissionOptions)=>this.shouldUseFeeSponsoring(submissionOptions),
            hasSourceAccountAuth: (preparedTx)=>this.hasSourceAccountAuth(preparedTx),
            sendAndPoll: (preparedTx, submissionOptions)=>this.sendAndPoll(preparedTx, submissionOptions),
            signResimulateAndPrepare: (hostFunc, authEntries, signOptions)=>this.signResimulateAndPrepare(hostFunc, authEntries, signOptions)
        }, tokenContract, recipient, amount, options);
    }
    // ==========================================================================
    // Private Helpers
    // ==========================================================================
    /**
     * Check if a transaction has any auth entries using source_account credentials.
     *
     * When auth uses source_account credentials, the authorization comes from the
     * transaction envelope signature, so we MUST sign even when using fee sponsoring.
     * For Address credentials, the authorization is in the auth entry itself.
     *
     * @param transaction - The transaction to check
     * @returns true if any auth entry uses source_account credentials
     * @internal
     */ hasSourceAccountAuth(transaction) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hasSourceAccountAuth"])(transaction);
    }
    /**
     * Simulate a host function to get auth entries
     */ async simulateHostFunction(hostFunc) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["simulateHostFunction"])({
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            deployerKeypair: this.deployerKeypair
        }, hostFunc);
    }
    /**
     * Sign auth entries with WebAuthn, re-simulate, and prepare transaction for submission.
     *
     * This is the core helper that handles the WebAuthn-specific flow:
     * 1. Sign each auth entry with the passkey
     * 2. Rebuild transaction with signed auth
     * 3. Re-simulate to get accurate resource costs (WebAuthn signatures are large)
     * 4. Assemble transaction with correct fees and soroban data
     *
     * @returns Prepared transaction ready for fee payer signature and submission
     */ async signResimulateAndPrepare(hostFunc, authEntries, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["signResimulateAndPrepare"])({
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            deployerKeypair: this.deployerKeypair,
            signAuthEntry: (entry, signOptions)=>this.signAuthEntry(entry, signOptions)
        }, hostFunc, authEntries, options);
    }
    /**
     * Determine which submission method to use based on configuration and options.
     *
     * Priority order (when not forced):
     * 1. Relayer (if configured)
     * 2. RPC (always available)
     *
     * @param options - Submission options
     * @returns The submission method to use
     */ getSubmissionMethod(options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getSubmissionMethod"])(this.relayer, options);
    }
    /**
     * Check if fee sponsoring service (Relayer) should be used.
     * When using fee sponsoring, transactions are wrapped in a fee-bump, so the
     * envelope signature is generally not required (unless source_account auth is present).
     */ shouldUseFeeSponsoring(options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["shouldUseFeeSponsoring"])(this.relayer, options);
    }
    /**
     * Send a transaction and poll for confirmation.
     *
     * Uses the following priority for submission (unless overridden):
     * 1. Relayer (if configured) - submits func + auth entries
     * 2. RPC (direct submission) - submits full transaction XDR
     *
     * @param transaction - The transaction to submit
     * @param options - Submission options
     * @returns Transaction result with hash and status
     */ async sendAndPoll(transaction, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$tx$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sendAndPoll"])({
            rpc: this.rpc,
            relayer: this.relayer
        }, transaction, options);
    }
    /**
     * Build a deployment transaction for the smart account contract
     * Returns an AssembledTransaction that can be signed and sent
     */ async buildDeployTransaction(credentialId, publicKey) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$deploy$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildDeployTransaction"])({
            accountWasmHash: this.accountWasmHash,
            webauthnVerifierAddress: this.webauthnVerifierAddress,
            networkPassphrase: this.networkPassphrase,
            rpcUrl: this.rpcUrl,
            deployerKeypair: this.deployerKeypair,
            timeoutInSeconds: this.timeoutInSeconds
        }, credentialId, publicKey);
    }
    // ==========================================================================
    // Multi-Signer Operations (private - access via kit.multiSigners.*)
    // ==========================================================================
    /**
     * Execute a transfer with multiple signers.
     * @internal Access via kit.multiSigners.transfer()
     */ async multiSignersTransfer(tokenContract, recipient, amount, selectedSigners, options) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$multi$2d$signer$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["multiSignersTransfer"])({
            getContractId: ()=>this._contractId,
            externalSigners: this.externalSigners,
            rpc: this.rpc,
            networkPassphrase: this.networkPassphrase,
            timeoutInSeconds: this.timeoutInSeconds,
            deployerKeypair: this.deployerKeypair,
            deployerPublicKey: this.deployerPublicKey,
            signAuthEntry: (entry, signOptions)=>this.signAuthEntry(entry, signOptions),
            shouldUseFeeSponsoring: (submissionOptions)=>this.shouldUseFeeSponsoring(submissionOptions),
            hasSourceAccountAuth: (preparedTx)=>this.hasSourceAccountAuth(preparedTx),
            sendAndPoll: (preparedTx, submissionOptions)=>this.sendAndPoll(preparedTx, submissionOptions)
        }, tokenContract, recipient, amount, selectedSigners, options);
    }
    // ==========================================================================
    // Utility Methods
    // ==========================================================================
    /**
     * Convert policy parameters to ScVal format for on-chain submission.
     *
     * When adding policies via `kit.policies.add()`, the install parameters need
     * to be in ScVal format. This method converts native JavaScript objects to
     * the proper ScVal format based on the policy type.
     *
     * @param policyType - The type of policy: "threshold", "spending_limit", or "weighted_threshold"
     * @param params - The policy parameters as a native JavaScript object
     * @returns The parameters converted to ScVal format, or the original params if conversion fails
     *
     * @example
     * ```typescript
     * // Convert threshold policy params
     * const thresholdParams = kit.convertPolicyParams("threshold", { threshold: 2 });
     *
     * // Convert spending limit params
     * const spendingParams = kit.convertPolicyParams("spending_limit", {
     *   token: "CDLZFC3...",
     *   limit: 1000000000n,
     *   period: 8640, // ~1 day in ledgers
     * });
     *
     * // Use with policies.add()
     * const tx = await kit.policies.add(ruleId, policyAddress, thresholdParams);
     * ```
     */ convertPolicyParams(policyType, params) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$policies$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["convertPolicyParams"])(this.wallet, policyType, params);
    }
    /**
     * Build a sorted policies Map as ScVal for on-chain submission.
     *
     * Soroban requires ScMap keys to be sorted. This method converts a JavaScript
     * Map of policy addresses to params into a properly sorted ScVal.
     *
     * @param policies - Map of policy addresses (C...) to their params
     * @param policyTypes - Map of policy addresses to their types (for conversion)
     * @returns ScVal representing the sorted policies map
     */ buildPoliciesScVal(policies, policyTypes) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2f$policies$2d$ops$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["buildPoliciesScVal"])(this.wallet, policies, policyTypes);
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/storage/localStorage.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "LocalStorageAdapter",
    ()=>LocalStorageAdapter
]);
/**
 * localStorage Storage Adapter
 *
 * Simple localStorage-based storage for credentials. Works in browsers
 * and provides persistence across page reloads.
 *
 * Note: localStorage has a 5MB limit and data is not encrypted.
 * For production use, consider IndexedDB or server-side storage.
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
const STORAGE_KEY = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LOCALSTORAGE_CREDENTIALS_KEY"];
const SESSION_KEY = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LOCALSTORAGE_SESSION_KEY"];
/**
 * Helper to serialize Uint8Array for JSON storage
 */ function serializeCredential(credential) {
    return {
        ...credential,
        publicKey: Array.from(credential.publicKey)
    };
}
/**
 * Helper to deserialize Uint8Array from JSON storage
 */ function deserializeCredential(data) {
    return {
        ...data,
        publicKey: new Uint8Array(data.publicKey)
    };
}
class LocalStorageAdapter {
    storageKey;
    constructor(storageKey = STORAGE_KEY){
        this.storageKey = storageKey;
    }
    getStorage() {
        if (typeof localStorage === "undefined") {
            throw new Error("localStorage is not available in this environment");
        }
        const data = localStorage.getItem(this.storageKey);
        if (!data) {
            return new Map();
        }
        try {
            const parsed = JSON.parse(data);
            const map = new Map();
            for (const [key, value] of Object.entries(parsed)){
                map.set(key, deserializeCredential(value));
            }
            return map;
        } catch  {
            return new Map();
        }
    }
    setStorage(credentials) {
        const obj = {};
        for (const [key, value] of credentials.entries()){
            obj[key] = serializeCredential(value);
        }
        localStorage.setItem(this.storageKey, JSON.stringify(obj));
    }
    async save(credential) {
        const credentials = this.getStorage();
        credentials.set(credential.credentialId, credential);
        this.setStorage(credentials);
    }
    async get(credentialId) {
        const credentials = this.getStorage();
        return credentials.get(credentialId) ?? null;
    }
    async getByContract(contractId) {
        const credentials = this.getStorage();
        const results = [];
        for (const credential of credentials.values()){
            if (credential.contractId === contractId) {
                results.push(credential);
            }
        }
        return results;
    }
    async getAll() {
        const credentials = this.getStorage();
        return Array.from(credentials.values());
    }
    async delete(credentialId) {
        const credentials = this.getStorage();
        credentials.delete(credentialId);
        this.setStorage(credentials);
    }
    async update(credentialId, updates) {
        const credentials = this.getStorage();
        const credential = credentials.get(credentialId);
        if (credential) {
            credentials.set(credentialId, {
                ...credential,
                ...updates
            });
            this.setStorage(credentials);
        }
    }
    async clear() {
        localStorage.removeItem(this.storageKey);
        localStorage.removeItem(SESSION_KEY);
    }
    async saveSession(session) {
        if (typeof localStorage === "undefined") {
            throw new Error("localStorage is not available in this environment");
        }
        localStorage.setItem(SESSION_KEY, JSON.stringify(session));
    }
    async getSession() {
        if (typeof localStorage === "undefined") {
            return null;
        }
        const data = localStorage.getItem(SESSION_KEY);
        if (!data) {
            return null;
        }
        try {
            return JSON.parse(data);
        } catch  {
            return null;
        }
    }
    async clearSession() {
        if (typeof localStorage !== "undefined") {
            localStorage.removeItem(SESSION_KEY);
        }
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/storage/indexeddb.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "IndexedDBStorage",
    ()=>IndexedDBStorage
]);
/**
 * IndexedDB Storage Adapter
 *
 * Recommended storage for web applications. Provides:
 * - Larger storage limits than localStorage
 * - Structured data with indexing
 * - Async API that doesn't block the main thread
 * - Better support for binary data (Uint8Array)
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
;
class IndexedDBStorage {
    dbName;
    dbPromise = null;
    constructor(dbName = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DB_NAME"]){
        this.dbName = dbName;
    }
    async getDB() {
        if (this.dbPromise) {
            return this.dbPromise;
        }
        this.dbPromise = new Promise((resolve, reject)=>{
            if (typeof indexedDB === "undefined") {
                reject(new Error("IndexedDB is not available in this environment"));
                return;
            }
            const request = indexedDB.open(this.dbName, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DB_VERSION"]);
            request.onerror = ()=>{
                reject(new Error(`Failed to open IndexedDB: ${request.error?.message}`));
            };
            request.onsuccess = ()=>{
                resolve(request.result);
            };
            request.onupgradeneeded = (event)=>{
                const db = event.target.result;
                // Create the credentials object store
                if (!db.objectStoreNames.contains(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"])) {
                    const store = db.createObjectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"], {
                        keyPath: "credentialId"
                    });
                    // Create indexes for efficient queries
                    store.createIndex(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_CONTRACT_ID"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_CONTRACT_ID"], {
                        unique: false
                    });
                    store.createIndex(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_CREATED_AT"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_CREATED_AT"], {
                        unique: false
                    });
                    store.createIndex(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_IS_PRIMARY"], __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_IS_PRIMARY"], {
                        unique: false
                    });
                }
                // Create the session object store (added in v2)
                if (!db.objectStoreNames.contains(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"])) {
                    db.createObjectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"], {
                        keyPath: "id"
                    });
                }
            };
        });
        return this.dbPromise;
    }
    async withStore(mode, callback) {
        const db = await this.getDB();
        return new Promise((resolve, reject)=>{
            const transaction = db.transaction(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"], mode);
            const store = transaction.objectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"]);
            const request = callback(store);
            request.onsuccess = ()=>resolve(request.result);
            request.onerror = ()=>reject(new Error(`IndexedDB operation failed: ${request.error?.message}`));
        });
    }
    async save(credential) {
        await this.withStore("readwrite", (store)=>store.put(credential));
    }
    async get(credentialId) {
        const result = await this.withStore("readonly", (store)=>store.get(credentialId));
        return result ?? null;
    }
    async getByContract(contractId) {
        const db = await this.getDB();
        return new Promise((resolve, reject)=>{
            const transaction = db.transaction(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"], "readonly");
            const store = transaction.objectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_CREDENTIALS"]);
            const index = store.index(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_INDEX_CONTRACT_ID"]);
            const request = index.getAll(contractId);
            request.onsuccess = ()=>resolve(request.result);
            request.onerror = ()=>reject(new Error(`Failed to query by contract: ${request.error?.message}`));
        });
    }
    async getAll() {
        return this.withStore("readonly", (store)=>store.getAll());
    }
    async delete(credentialId) {
        await this.withStore("readwrite", (store)=>store.delete(credentialId));
    }
    async update(credentialId, updates) {
        const existing = await this.get(credentialId);
        if (existing) {
            await this.save({
                ...existing,
                ...updates
            });
        }
    }
    async clear() {
        await this.withStore("readwrite", (store)=>store.clear());
        await this.clearSession();
    }
    async saveSession(session) {
        const db = await this.getDB();
        return new Promise((resolve, reject)=>{
            const transaction = db.transaction(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"], "readwrite");
            const store = transaction.objectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"]);
            const request = store.put({
                id: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_SESSION_KEY"],
                ...session
            });
            request.onsuccess = ()=>resolve();
            request.onerror = ()=>reject(new Error(`Failed to save session: ${request.error?.message}`));
        });
    }
    async getSession() {
        const db = await this.getDB();
        return new Promise((resolve, reject)=>{
            const transaction = db.transaction(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"], "readonly");
            const store = transaction.objectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"]);
            const request = store.get(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_SESSION_KEY"]);
            request.onsuccess = ()=>{
                if (request.result) {
                    const { id, ...session } = request.result;
                    resolve(session);
                } else {
                    resolve(null);
                }
            };
            request.onerror = ()=>reject(new Error(`Failed to get session: ${request.error?.message}`));
        });
    }
    async clearSession() {
        const db = await this.getDB();
        return new Promise((resolve, reject)=>{
            const transaction = db.transaction(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"], "readwrite");
            const store = transaction.objectStore(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_STORE_SESSION"]);
            const request = store.delete(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["IDB_SESSION_KEY"]);
            request.onsuccess = ()=>resolve();
            request.onerror = ()=>reject(new Error(`Failed to clear session: ${request.error?.message}`));
        });
    }
    /**
     * Close the database connection
     */ async close() {
        if (this.dbPromise) {
            const db = await this.dbPromise;
            db.close();
            this.dbPromise = null;
        }
    }
    /**
     * Delete the entire database
     */ static async deleteDatabase(dbName = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DB_NAME"]) {
        return new Promise((resolve, reject)=>{
            const request = indexedDB.deleteDatabase(dbName);
            request.onsuccess = ()=>resolve();
            request.onerror = ()=>reject(new Error(`Failed to delete database: ${request.error?.message}`));
        });
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/storage/index.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
/**
 * Storage Adapters for Credential Persistence
 *
 * This module provides various storage implementations for persisting
 * WebAuthn credential data (including the crucial credential IDs).
 */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$memory$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/storage/memory.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$localStorage$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/storage/localStorage.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$indexeddb$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/storage/indexeddb.js [app-client] (ecmascript)");
;
;
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/wallet-adapter.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * External Wallet Adapter Implementations
 *
 * This module provides concrete adapter implementations for integrating
 * external Stellar wallets with the Smart Account Kit.
 *
 * @remarks
 * To use the StellarWalletsKitAdapter, you must install the peer dependency:
 * ```bash
 * pnpm add @creit-tech/stellar-wallets-kit
 * ```
 */ /**
 * Adapter for StellarWalletsKit
 *
 * This adapter integrates the @creit-tech/stellar-wallets-kit library
 * with the Smart Account Kit, providing a unified interface for
 * connecting to and signing with external Stellar wallets like
 * Freighter, Lobstr, xBull, and others.
 *
 * @remarks
 * Requires `@creit-tech/stellar-wallets-kit` to be installed as a peer dependency.
 *
 * @example
 * ```typescript
 * import { StellarWalletsKitAdapter, SmartAccountKit } from "smart-account-kit";
 * import { Networks } from "@stellar/stellar-sdk";
 *
 * // Create and initialize the adapter
 * const walletAdapter = new StellarWalletsKitAdapter({
 *   network: Networks.TESTNET
 * });
 * await walletAdapter.init();
 *
 * // Use with SmartAccountKit
 * const kit = new SmartAccountKit({
 *   rpcUrl: "https://soroban-testnet.stellar.org",
 *   networkPassphrase: Networks.TESTNET,
 *   externalWallet: walletAdapter,
 * });
 *
 * // Connect a wallet
 * const wallet = await walletAdapter.connect();
 * if (wallet) {
 *   console.log(`Connected: ${wallet.walletName} (${wallet.address})`);
 * }
 * ```
 */ __turbopack_context__.s([
    "StellarWalletsKitAdapter",
    ()=>StellarWalletsKitAdapter
]);
class StellarWalletsKitAdapter {
    network;
    connectedWallets = new Map();
    onConnectionChange;
    StellarWalletsKit = null;
    initialized = false;
    constructor(config = {}){
        // Default to testnet if not specified
        this.network = config.network || "Test SDF Network ; September 2015";
        this.onConnectionChange = config.onConnectionChange;
    }
    /**
     * Initialize the adapter
     *
     * Must be called before using connect() or signAuthEntry().
     * This method imports StellarWalletsKit and initializes it with
     * all SEP-43 compatible wallet modules.
     *
     * @throws Error if @creit-tech/stellar-wallets-kit is not installed
     */ async init() {
        if (this.initialized) return;
        try {
            // Dynamic imports - will fail if peer dependency not installed
            const { StellarWalletsKit } = await Promise.resolve().then(()=>{
                const e = new Error("Cannot find module '@creit-tech/stellar-wallets-kit'");
                e.code = 'MODULE_NOT_FOUND';
                throw e;
            });
            const { sep43Modules } = await Promise.resolve().then(()=>{
                const e = new Error("Cannot find module '@creit-tech/stellar-wallets-kit/modules/utils'");
                e.code = 'MODULE_NOT_FOUND';
                throw e;
            });
            // Initialize StellarWalletsKit with all SEP-43 compatible modules
            StellarWalletsKit.init({
                modules: sep43Modules(),
                network: this.network
            });
            this.StellarWalletsKit = StellarWalletsKit;
            this.initialized = true;
        } catch (error) {
            throw new Error(`Failed to initialize StellarWalletsKitAdapter. ` + `Make sure @creit-tech/stellar-wallets-kit is installed: pnpm add @creit-tech/stellar-wallets-kit`);
        }
    }
    /**
     * Ensure the adapter is initialized and return the StellarWalletsKit instance
     * @internal
     */ ensureInitialized() {
        if (!this.initialized || !this.StellarWalletsKit) {
            throw new Error("StellarWalletsKitAdapter not initialized. Call init() first.");
        }
        return this.StellarWalletsKit;
    }
    /**
     * Get list of available/installed wallet extensions
     *
     * @returns Array of wallet info with availability status
     */ async getAvailableWallets() {
        const kit = this.ensureInitialized();
        return kit.refreshSupportedWallets();
    }
    /**
     * Connect to a wallet using the built-in modal
     *
     * Shows a modal allowing the user to select and connect their preferred wallet.
     * The connected wallet is tracked internally for signing operations.
     *
     * @returns Connected wallet info, or null if user cancelled
     */ async connect() {
        const kit = this.ensureInitialized();
        try {
            const result = await kit.authModal();
            if (result?.address) {
                const selectedModule = kit.selectedModule;
                const wallet = {
                    address: result.address,
                    walletId: selectedModule?.productId ?? "unknown",
                    walletName: selectedModule?.productName ?? "Unknown Wallet"
                };
                // Track connected wallet
                this.connectedWallets.set(wallet.address, wallet);
                // Notify listeners
                this.onConnectionChange?.(true, wallet);
                return wallet;
            }
            return null;
        } catch  {
            // Connection failed - return null to indicate failure
            // Callers can handle this gracefully
            return null;
        }
    }
    /**
     * Reconnect to a previously connected wallet by ID
     *
     * Attempts to reconnect to a specific wallet type without showing the modal.
     * This is used for restoring connections on page reload.
     *
     * If the wallet extension remembers the site authorization, this will
     * succeed silently. Otherwise, it may prompt the user for authorization.
     *
     * @param walletId - The wallet ID to reconnect to (e.g., 'freighter', 'lobstr')
     * @returns Connected wallet info, or null if reconnection failed
     */ async reconnect(walletId) {
        const kit = this.ensureInitialized();
        try {
            // Set the wallet module to use
            kit.setWallet(walletId);
            // Try to get the address - this will work if the wallet remembers the site
            const result = await kit.getAddress();
            if (result?.address) {
                const selectedModule = kit.selectedModule;
                const wallet = {
                    address: result.address,
                    walletId: selectedModule?.productId ?? walletId,
                    walletName: selectedModule?.productName ?? walletId
                };
                // Track connected wallet
                this.connectedWallets.set(wallet.address, wallet);
                // Notify listeners
                this.onConnectionChange?.(true, wallet);
                return wallet;
            }
            return null;
        } catch  {
            // Reconnection failed - wallet may not be installed or not authorized
            return null;
        }
    }
    /**
     * Disconnect all wallets
     */ async disconnect() {
        const kit = this.ensureInitialized();
        await kit.disconnect();
        this.connectedWallets.clear();
        this.onConnectionChange?.(false);
    }
    /**
     * Disconnect a specific wallet by address
     *
     * @param address - The Stellar address to disconnect
     */ disconnectByAddress(address) {
        this.connectedWallets.delete(address);
        if (this.connectedWallets.size === 0) {
            this.onConnectionChange?.(false);
        }
    }
    /**
     * Sign an auth entry with the wallet
     *
     * @param authEntryXdr - The auth entry to sign, as XDR string
     * @param opts - Optional signing options
     * @returns The signed auth entry
     * @throws Error if no wallet is connected or requested address not found
     */ async signAuthEntry(authEntryXdr, opts) {
        const kit = this.ensureInitialized();
        const targetAddress = opts?.address;
        // Validate we can sign for the requested address
        if (targetAddress && !this.connectedWallets.has(targetAddress)) {
            throw new Error(`No wallet connected for address ${targetAddress}`);
        }
        if (!targetAddress && this.connectedWallets.size === 0) {
            throw new Error("No wallet connected");
        }
        // Use the target address or first connected wallet
        const signingAddress = targetAddress || [
            ...this.connectedWallets.keys()
        ][0];
        return kit.signAuthEntry(authEntryXdr, {
            networkPassphrase: opts?.networkPassphrase || this.network,
            address: signingAddress
        });
    }
    /**
     * Check if we can sign for a specific address
     *
     * @param address - The Stellar address to check
     * @returns True if a wallet is connected for this address
     */ canSignFor(address) {
        return this.connectedWallets.has(address);
    }
    /**
     * Get wallet info for a specific address
     *
     * @param address - The Stellar address to look up
     * @returns Wallet info if connected, undefined otherwise
     */ getWalletForAddress(address) {
        return this.connectedWallets.get(address);
    }
    /**
     * Get all connected wallets
     *
     * @returns Array of connected wallet info
     */ getConnectedWallets() {
        return [
            ...this.connectedWallets.values()
        ];
    }
    /**
     * Check if any wallet is connected
     */ get isConnected() {
        return this.connectedWallets.size > 0;
    }
}
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
"[project]/node_modules/smart-account-kit/dist/index.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
/**
 * Smart Account Kit
 *
 * TypeScript SDK for deploying and managing OpenZeppelin Smart Account contracts
 * on Stellar/Soroban with WebAuthn passkey authentication.
 *
 * @packageDocumentation
 */ // Main client SDK
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$kit$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/kit.js [app-client] (ecmascript)");
// External signer types
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$external$2d$signers$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/external-signers.js [app-client] (ecmascript)");
// Storage adapters
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$storage$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/storage/index.js [app-client] (ecmascript) <locals>");
// Constants (public API - implementation details are internal)
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/constants.js [app-client] (ecmascript)");
// Error classes
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/errors.js [app-client] (ecmascript)");
// Utility functions (public API - low-level helpers are internal)
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/utils.js [app-client] (ecmascript)");
// Builder utilities
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$builders$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/builders.js [app-client] (ecmascript) <locals>");
// Event emitter
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$events$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/events.js [app-client] (ecmascript)");
// Indexer client for reverse lookups
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$indexer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/indexer.js [app-client] (ecmascript)");
// Relayer client for fee-sponsored transactions via proxy
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$relayer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/relayer.js [app-client] (ecmascript)");
// Wallet Adapters
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$smart$2d$account$2d$kit$2f$dist$2f$wallet$2d$adapter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/smart-account-kit/dist/wallet-adapter.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
if (typeof globalThis.$RefreshHelpers$ === 'object' && globalThis.$RefreshHelpers !== null) {
    __turbopack_context__.k.registerExports(__turbopack_context__.m, globalThis.$RefreshHelpers$);
}
}),
]);

//# sourceMappingURL=node_modules_smart-account-kit_dist_0o-i8nz._.js.map